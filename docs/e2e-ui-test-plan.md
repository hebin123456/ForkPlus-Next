# ForkPlus-Next 全功能 UI 自动化测试计划

> 固化日期：2026-09-05。本文件是全功能测试的唯一进度真相源（single source of truth）。
> 新 agent 接手：读「接手指南」→ 按「进度追踪」表找到下一个待做模块 → 按「测试基建约定」写测试。

## 接手指南

1. 进度以本文档「进度追踪」表为准：`未开始` → `进行中` → `完成`（每完成一个模块立即更新本表并 git push）。
2. 所有测试代码放 `src/ForkPlus.Tests/`，文件命名 `E2e<模块名>Tests.cs`。
3. 截图证据存 `docs/evidence/e2e/<两位模块号>-<模块短名>/<场景>.png`，随代码入库。
4. 测试写法严格遵循「测试基建约定」；禁止截图识别定位控件（用控件树查找 + RaiseEvent）。
5. 一次提交只做一个模块（或基建步骤），commit message 格式：`test(e2e): <模块> <一句话>`。
6. **🚨 推送一律 `git push origin master`**（本仓库只用 `master` 分支，远程 `main` 已于 2026-09-06 删除——此前多个 agent 误推过 `main`，勿再犯）。
7. 每个模块完成后：跑该测试类 → 全绿 → 截图入库 → 更新本表 → commit+push（凭据见仓库 remote，勿写入代码）。
8. 全部完成后生成 `docs/evidence/e2e/index.html` 截图索引报告。

## 测试基建约定（阶段 0 已建）

### 已提供的基础设施

| 类 | 文件 | 用途 |
|----|------|------|
| `HeadlessAppBootstrap` | `HeadlessAppBootstrap.cs`（已有） | 无头 App 启动，真实主题资源 + 真 Skia 渲染 |
| `TestRepoFactory` | `TestRepoFactory.cs`（新建） | 构造各形态临时 git 仓库（多提交/分支/tag/stash/冲突/LFS/bare 远程…） |
| `ScreenshotHelper` | `ScreenshotHelper.cs`（新建） | 统一截图入库路径 + 非空白像素断言 + 前后差异断言 |
| `UiClick` | `UiClick.cs`（新建） | 控件树查找（按类型/名字）、点击（RaiseEvent）、输入、等待渲染 |

### 标准测试骨架

```csharp
[Collection("HeadlessAvalonia")]
public class E2eXxxTests
{
    [Fact]
    public void Xxx_Scenario_Yyy()
    {
        HeadlessAppBootstrap.EnsureStarted();
        string repo = TestRepoFactory.CreateBasic(); // 按需选形态
        try
        {
            HeadlessAppBootstrap.Run(delegate
            {
                // 1. 构造/打开真实控件
                // 2. UiClick.Find<T>(root, "Name") / UiClick.Click(btn) 交互
                // 3. ScreenshotHelper.Snap(window, "01-初始", "01-sidebar");
            });
            // 断言状态变化（不只截图）
        }
        finally { TestRepoFactory.Cleanup(repo); }
    }
}
```

### 铁律

- 点击用 `RaiseEvent(new RoutedEventArgs(Button.ClickEvent))` 或 headless 输入，**不用**截图识别。
- 每张截图必须配至少一条状态断言（截图非空白 + 交互效果）。
- 涉及真实 git 执行的用例，仓库建在临时目录，`finally` 清理。
- 不可自动化项（账号登录/公网）降级为 UI 冒烟：窗口能开、表单校验、取消路径。
- **环境依赖（模块 17 起）**：GitFlow 用例依赖 gitflow-avh（源码安装于 `~/.local/bin`，已写入 `~/.bashrc`）——跑测试的 shell 须 `export PATH="$HOME/.dotnet:$HOME/.local/bin:$PATH"`（git 查找 git-flow 走 PATH 而非 exec-path）；git-mm CLI 沙箱没有，GitMm 四命令窗口（Start/Reference/Sync/Upload）记环境不可测。

### 截图口径（2026-09-05 用户约定，自模块 10 落地）

- **统一 1920×1280 最大化截图**：`ScreenshotHelper.Snap` 内部自动"放大窗口到 1920×1280 → 布局 → 截帧 → 复原窗口尺寸与全部滚动容器偏移"，调用方无感知。模块 1-9 的证据已随模块 10 的全量回归按新口径重生成（96/99 张精确 1920×1280；其余 3 张为高度受内容约束的窗口——欢迎页条形窗 1920×350、丢弃确认小弹窗 1920×156——宽度放大到 1920、高度保持内容自然高度，属"最大化"语义下受限窗口的真实形态）。
- 复原滚动偏移是硬要求：放大瞬间 viewport 超过文档 extent 会把 ScrollViewer.Offset 钳到 0（AvaloniaEdit TextView 布局期钳制），不复原会破坏截图点之后仍要断言滚动位置的用例。
- **宽度封顶窗口（模块 11 补充）**：`SizeToContent=WidthAndHeight` 的窗口（如 `CheckoutBranchWindow`，`MaxWidth=670`，WPF 原仓同款）内容驱动宽度会无视显式 Width 回缩自然宽——截图时临时降为 `Height` 让显式 Width 生效，仍受窗口自身 `MaxWidth` 钳制（= 窗口允许的最大宽，670×自然高）。既定口径下"受限窗口按自身极限渲染"的又一形态。

### Bug 修复策略（2026-09-05 用户约定）

- **测试用例发现生产代码 bug → 必须修复源码**（不是绕过或放宽断言）；修复随该模块的测试提交一起入库，并在变更日志里记"生产 bug 修复"条目（含根因）。
- 判定标准：与 WPF 原仓（`ForkPlus`）行为不一致 = 迁移 bug，修源码；与原仓一致 = 原始行为，测试按原版语义断言并在用例注释里注明"非迁移回归"（对照原仓文件路径/行为依据）。已有先例：模块 5 自动补全浮层渲染类名（模板 Resources 迁移缺失，修源码）、模块 7 横向滚动条弹动（防抖缺失，修源码）；模块 10 的 Next/Prev 最小滚动分数抑制（AvalonEdit/AvaloniaEdit 同源算法，原版行为，测试适配）。

## 模块规划与进度追踪

优先级 P0（主界面高频路径）→ P1（Diff 系统，含回归）→ P2（Git 工作流对话框）→ P3（设置与辅助）。

### 阶段 0：基建

| # | 任务 | 状态 |
|---|------|------|
| 0.1 | TestRepoFactory（基础/多分支/冲突/stash/bare远程/LFS 形态） | 完成 |
| 0.2 | ScreenshotHelper + UiClick | 完成 |
| 0.3 | 计划文档（本文件） | 完成 |

### P0 核心主界面

| # | 模块 | 覆盖内容 | 预计截图 | 状态 |
|---|------|---------|---------|------|
| 1 | 启动与仓库管理 | 欢迎页、仓库列表器（分组/搜索）、打开仓库、Tab 增删切换 | ~10 | 完成 |
| 2 | 提交历史视图 | 图表渲染、行选择/多选、搜索面板、tooltip、滚动虚拟化 | ~15 | 完成 |
| 3 | 侧边栏 | 分支/标签/stash/远程/子模块分组展开收起、过滤、右键菜单 | ~12 | 完成 |
| 4 | 修订详情 | 变更 tab、文件树 tab、摘要、reflog/tags 隐藏开关 | ~8 | 完成 |
| 5 | 变更与提交 | 暂存/取消暂存、行级 chunk stage/discard 浮窗、提交消息自动补全 | ~15 | 完成 |
| 6 | 工具栏 | push/pull/fetch/stash 按钮状态（领先/落后角标） | ~6 | 完成 |

### P1 Diff 系统（近期修复集中区）

| # | 模块 | 覆盖内容 | 预计截图 | 状态 |
|---|------|---------|---------|------|
| 7 | 文本 Diff | Split/SideBySide 切换、垂直+水平滚动同步（**横向滚动条弹动修复回归**） | ~10 | 完成 |
| 8 | DiffPopupWindow | 提交/变更两入口（文件树 tab 无弹窗入口，为内联预览）、横向滚动条、Escape/Space 关闭、上下键换文件 | ~6 | 完成 |
| 9 | 二进制 Diff | Hex 视图（图片内切换 + 非图片二进制直发两路径）、图片对比（SideBySide/Swipe 拖分割线/OnionSkin 滑块/HighlightPixels） | ~8 | 完成 |
| 10 | 合并冲突窗口 | 三方编辑器、冲突块选择、全选 ours/theirs、滚动同步回归 | ~10 | 完成 |

### P2 Git 工作流对话框

| # | 模块 | 覆盖内容 | 预计截图 | 状态 |
|---|------|---------|---------|------|
| 11 | 分支操作 | 创建/检出/重命名/删除/跟踪/多分支推送，9 窗口 | ~20 | 完成 |
| 12 | 标签操作 | 创建/删除/推送/多标签推送/详情，5 窗口 | ~10 | 完成 |
| 13 | 历史改写 | 合并/变基/交互式变基真实仓库端到端/拣选/还原/重置/Reflog | ~18 | 完成 |
| 14 | Stash | 保存/部分保存/应用/删除/重命名，5 窗口 | ~10 | 完成 |
| 15 | 远程交互 | Fetch/Pull/Push/编辑远程/自定义 refspec（对本地 bare 真实执行） | ~15 | 完成 |
| 16 | 子模块与 Worktree | 添加/删除子模块、子模块 diff、worktree 三窗口 | ~12 | 完成 |
| 17 | 工作流套件 | GitFlow（init+start/finish×3）、GitMm、LeanBranching | ~20 | 完成 |
| 18 | Git LFS | track/status/fetch/pull，4 窗口 | ~6 | 完成 |
| 19 | 补丁与快照 | 保存为 patch/应用 patch/快照，3 窗口 | ~6 | 完成 |
| 20 | 查看类窗口 | Blame/文件历史/仓库概览/统计/修订详情独立窗/GoToLine | ~12 | 完成 |

### P3 设置与辅助

| # | 模块 | 覆盖内容 | 预计截图 | 状态 |
|---|------|---------|---------|------|
| 21 | 偏好设置 | General/Git/Commit/Integration/ImportExport/CustomCommands/AI | ~8 | 完成 |
| 22 | 仓库设置 | General/IssueTracker/CommitTemplate/CustomCommands本地模式 | ~5 | 完成 |
| 23 | SSH 与环境 | SSH 密钥/生成密钥/Git 实例/Workspace | ~6 | 完成 |
| 24 | AI 功能 | AI 提交/代码审查/开发助手（UI + stub） | ~6 | 完成 |
| 25 | 通用对话框 | MessageBox/Error/AskPass/颜色/快捷键/About/更新/诊断/通知 | ~14 | 未开始 |

### 明确降级项（UI 冒烟即可）

真实账号登录窗（GitHub/GitLab/Bitbucket×2/Gitea/GitHubEnterprise/OpenAI 七个）、公网 push/pull、PR/Issue 集成、应用更新下载 —— 只验证窗口打开 + 表单校验 + 取消路径。

## 变更日志

- 2026-09-05：计划固化为本文档；阶段 0 基建（TestRepoFactory/ScreenshotHelper/UiClick）完成。
- 2026-09-05：模块 1 完成（E2e01WelcomeAndRepoManagerTests：欢迎页表单、仓库列表选中/空态、Tab 增删选切换；截图 01-welcome/）。
- 2026-09-05：模块 2 完成（E2e02RevisionListTests：真实 git 管线加载、行选中、上下文搜索匹配标记/清空恢复、方向切换；截图 02-revisionlist/）。基建补强：HeadlessAppBootstrap 补 ServiceLocator 初始化（修复 headless 下 DelayedAction 回调被静默丢弃）、UiClick 新增 WaitFor 轮询等待。
- 2026-09-05：模块 3 完成（E2e03SidebarTests：分组结构断言（main 活跃分支/feature 嵌套文件夹/tags）、文件夹与分组展开收起（生产 IsExpanded setter 完整管线）、过滤防抖→Refilter→IsHidden、清空恢复、分支右键菜单（真实 PointerPressed 管线）、分组右键菜单、stash 分组；截图 03-sidebar/，9 张）。
- 2026-09-05：模块 4 完成（E2e04RevisionDetailsTests：三 tab（Commit 摘要/Changes 变更/File Tree 文件树）真实 git 数据加载与切换、摘要字段断言、diff 内容加载（ParsedDiffContent）、文件树选择→内容预览、Range 双提交对比（Commit/FileTree 禁用+自动切 Changes）、reflog 开关（状态栏文案语言无关断言）、tag 引用过滤（Filtered by 状态栏）、HideTags 开关往返；截图 04-revisiondetails/，8 张）。测试经验：行选中须用 RevisionListViewUserControl.Select（补发 NotifySelectionChangedFromCurrentItems），直接调 DragAndDropListView.Select 会被 IsMultiselectionInProgress 吞掉通知；同秒提交行序不保证，按 Subject 扫行定位。
- 2026-09-05：模块 5 第一批（E2e05ChangesAndCommitTests 三个用例 + E2eMainWindowHarness 基建）：真实 MainWindow 生产路径打开仓库（TabManager.OpenRepository，解决 headless 下 IsActiveRepository()==null 导致 Commit 视图状态刷新被 _pendingRepositoryStatusUiRefresh 推迟的核心阻塞）、工作区状态装配（修改/删除/未跟踪/已暂存四形态）、选中文件 working dir diff 加载、Stage/Unstage 选中文件（UI 列表 + git index 双重验证）、StageAllButton 智能切换 Stage All ↔ Unstage All；截图 05-changescommit/ 前 5 张。基建新增：TestRepoFactory.CreateWorkingDir（工作区四形态仓库）+ GitOutput（git stdout 断言助手）、E2eMainWindowHarness（真实 MainWindow 挂具：OpenRepository/CloseRepositoryTab/Tr/TrFormat）。测试经验：① 绝不能 Close() MainWindow（Closed→lifetime.Shutdown 会关停 headless App），收尾只 CloseRepositoryTab；② SelectFile 对已选中项会重复 Add 进 TreeView.SelectedItems（生产怪癖），数量断言会得 2，须用去重路径集合；③ Commit 按钮文案走 FormatCurrent（键含 {0} 占位符），断言用 TrFormat("Commit {0} File", n) 而非 Tr("Commit 1 File")；④ 取消暂存 c.txt 后它仍相对 HEAD 有改动 → 回到未暂存列表，Unstage All 期望是 4 项不是 3 项。
- 2026-09-05：模块 5 第二批（同文件再增三个用例，模块完成）：行级 chunk stage 浮窗（Select+CaptureRenderedFrame 逼真渲染帧 → DiffSelectionLayer 悬浮按钮 Stage/Discard... 出现 → 点 Stage → ApplyChunkCommand → git apply --cached 部分补丁，UI+`git diff --cached` 双验证：仅 line4 入暂存、line5 留工作区）；行级 discard 浮窗（点 Discard... → MessageBoxWindow.ShowDialog DispatcherFrame 模态泵，Post 泵内驱动：找确认框→截图→点"丢弃此行"→反向补丁生效，a.txt 内容 + git diff 双断言）；提交消息自动补全（描述框输入 Co-auth → 30ms 防抖 → Popup 弹出 Co-authored-by: 建议 → Tab 选中替换 → caret 定位/浮层关闭/FullCommitMessage 拼装）。截图 05-changescommit/ 补至 11 张。测试经验：① git diff 断言必须用 `+行文本` 前缀匹配（context 行也含原文本，纯 Contains 会误判）；② Popup 内容挂独立 PopupRoot，ListBox 从 popup.Child 直取（视觉树搜索找不到）；③ FullCommitMessage setter 走 DisableUpdates 静默写不触发建议，须对 AutoCompleteTextBox.Text 直接赋值模拟输入；④ 建议浮层的 ItemTemplate 断言防类名渲染回归。
- 2026-09-05（生产 bug 修复，E2e05 发现）：自动补全建议浮层显示类名 `AutoCompleteSuggestion` 而非建议文本。根因：原 WPF 三个按 DataType 的 DataTemplate 放在 ListBoxItem ControlTemplate.Resources 内，Avalonia ControlTemplate 无 Resources，迁移时模板被注释化但代码侧查找键 "AutocompleteListBoxItemTemplate" 保留 → ItemTemplate=null → ToString() 渲染。修复：Listview.axaml 恢复三个模板为顶层资源 + 新增 AutoCompleteSuggestionTemplateSelector（IDataTemplate 按运行时类型路由，Gitmoji 条目/用户身份带图标）。同批修复测试基建：E2eMainWindowHarness 收尾 Hide+清 lifetime.MainWindow——残留可见 MainWindow 参与后续主题测试布局时 ModernTabControl 模板重建抛 "already has a visual parent"（E2e05 先跑则 ThemeSystemIntegrityTests 必崩，实证为既有交互缺陷非本次回归）。
- 2026-09-05：模块 6 完成（E2e06ToolbarTests 三个用例）：无仓库 tab 全部 git 操作按钮禁用 + Undo/Redo 可见性跟随设置 + 角标隐藏（RefreshToolbar isEnabled=false 分支）、无 upstream 仓库按钮全启用 + 角标隐藏、ahead/behind 仓库 PullBadge "1"/PushBadge "2" 数字 + Canvas 定位断言（RefreshRepositoryData → UpdateRepositoryData → RefreshToolbarBadges 全链路真实 git 管线，侧边栏 2↑1↓ 与角标一致）；截图 06-toolbar/ 3 张。基建新增：TestRepoFactory.CreateAheadBehind（bare 远程 + 双克隆交叉推送：ahead 2 + behind 1，UpstreamStatus 基于本地 remote-tracking ref 无需网络）、harness CreateWindow（不开仓库）+ DetachWindow（窗口摘除）+ RemoveTestReposFromManager（测试仓库从"最近"列表摘除——OpenRepository 经 AddOrUpdateLastOpened 持久化到用户配置，残留 fpe2e_* 条目会污染真实用户机器；顺带清历史残留）。
- 2026-09-05：模块 7 完成（E2e07TextDiffTests 两个用例）：Split ↔ SideBySide 布局切换（生产入口：diff 头部 DiffLayoutModeToggleButton 真实点击序 IsChecked+Click → 设置 + NotificationCenter.DiffLayoutModeChanged → TextDiffControl.RefreshLayout 重建 → SideBySideCommitTextDiffControl 双编辑器 old/new + VisualPatch 内容迁移断言）、SideBySide 垂直滚动同步（右滚 100px → ScrollOffsetChanged → OnScrollOffsetChanged → 左跟随，偏移差 <1px）+ 水平滚动同步 + 交替滚动弹动回归（2026-09-05"点击横向滚动条界面弹动"修复：防抖 100ms + 差值检查，交替滚 5 轮收敛一致无循环）；截图 07-textdiff/ 5 张。测试经验：① 滚动编辑器必须用 ScrollToVerticalOffsetCompat/ScrollToHorizontalOffsetCompat（AvaloniaEdit 12.x 的 TextEditor.ScrollTo*Offset 是空操作，生产同步也走 compat）；② 测试仓库构造要懂 diff 语义——Commit diff 默认 hunk 视图，只改 1 行时 diff 仅 7 行无垂直滚动范围（须整文件大改动造大 hunk），宽行要放文件顶部（AvaloniaEdit TextView 宽度 extent 只由可见行决定，宽行滚出视野时水平 extent 塌缩、水平滚动范围消失）；③ **持久化设置污染**：测试改 ForkPlusSettings 后 harness 会把修改值落盘（CloseRepositoryTab→SaveSession），finally 只恢复内存不落盘 → 磁盘残留 SideBySide → 后续运行 E2e05 行级测试拿到 SideBySide 左侧旧内容编辑器而失败——修复：测试 finally 恢复后调 ForkPlusSettings.Default.Save() 落盘，且对布局敏感的测试（E2e05 行级）开头显式固定 Split。
- 2026-09-05：模块 8 完成（E2e08DiffPopupWindowTests 四个用例）：Commit 视图 Space 打开弹窗（生产入口：文件列表 KeyDown Tunnel → ShowDiffPopup 事件 → CommitUserControl.ShowDiffPopup → CreateCommitDiff → ShowAtCenter(父窗 90%) → UpdateDiff 同步行内内容；断言 Title=路径、CommitFileDiffControl、弹窗 90% 尺寸、CommitCodeEditor+VisualPatch 装配）+ Escape 关闭（Tunnel KeyDown）+ Space 重开（Closed → _diffPopupWindow 置空）+ 弹窗内 Space 关闭（bubble CLR KeyDown 路径）；长行弹窗水平滚动范围（PART_ScrollViewer.ScrollBarMaximum.X > 100）+ ScrollToHorizontalOffsetCompat 滚动到位；弹窗内 Down/Up 换文件（Tunnel → SelectNext/SelectPrevious → TreeView 选择变化 → 异步 git diff → popup.UpdateDiff → Title/内容/主视图选择三重同步断言）；变更视图（修订详情 Changes tab）Space 打开修订弹窗（CreateRevisionDiff → 基类 FileDiffControl 而非 CommitFileDiffControl、DiffCodeEditor 非空文档）。计划修正：文件树 tab 无弹窗入口（内联预览，模块 4 已覆盖）。截图 08-diffpopup/ 6 张。测试经验：**等待条件不能是"状态 != 初值"**——SelectNextFile 先 TreeView.SelectedItems.Clear()（同步 SelectionChanged → UpdateDiff(null) → Title 瞬时变 "File Preview"）再 SelectAndFocus(下一文件)，"Title != a.txt" 在全量负载时会抓到瞬时中间态而单独跑时碰不到（时序依赖假绿）——必须等"状态 == 期望终值"；另：弹窗泄漏进 lifetime.Windows 会殃及后续用例，finally 必须 CloseLeftoverPopups。
- 2026-09-05：模块 9 完成（E2e09BinaryDiffTests 两个用例）：图片对比全链路（Commit 视图选 img.png → FileDiffControl 二进制+IsImagePath 分发 → BinaryDiffUserControl；360x240 绿→橙大图，四视图逐一切换）——Side-by-Side 默认（old/new 双内容控件 + DiffImageSource 品红差异图生成 → header HighlightPixels 开关启用）、Swipe 分割线**真实窗口级指针拖拽**（MouseDown/Move/MouseUp → GridSplitter → 列宽 → SizeChanged → RefreshClipX → OverlayImage.ClipX 左拖后显著减小）、OnionSkin 透明度滑块（Slider.Value=0.5 → ValueChanged → NewOpacity=0.5；截图实证橄榄绿=50%绿+50%橙正确混合）、HighlightPixels（header 开关生产点击序 IsChecked+Click → 设置 + NotificationCenter.ImageDiffHighlightPixelsChanged → 双视图 OverlayImage.HighlightImageDiff 同步）、图片切 Hex（懒创建 HexDiffUserControl + 双 HexEditor 字节不同）；非图片二进制（data.bin 256→512B）→ FileDiffControl 直发 HexDiffUserControl（CanLoadHexDiff 小文件路径，无 BinaryDiffUserControl）。截图 09-binarydiff/ 8 张。**测试基建 bug 修复（探针实证）**：TestRepoFactory 的 PNG 生成器（MakeSolidPng 及新增 MakePngBytes）块序为 [type][length] 且 CRC 覆盖 length+data，违反 PNG 规范 [length][type][data][CRC]（CRC 覆盖 type+data）——Skia "Unable to load bitmap from provided data" 拒绝解码；从未暴露因为此前没有任何测试真正解码过生成图片。已同步修复两个生成器（IEND 常量的正确序即规范印证）。经验：**手写二进制格式必须用解码探针闭环验证**（生成→真实解码器消费），否则坏数据混进测试资产直到第一个真正解码的测试出现才炸。
- 2026-09-05：模块 10 第一批（E2e10MergeConflictTests 五个用例，5 落地 2 过 3 失败根因已定位）：MergeConflictUserControl 装配与 ResolveButton 三态（Merge/Choose {0}/禁用）✔、Choose theirs 主入口解决 ✔（checkout-index --stage=3 + git add → "M " 暂存迁移 UI+git 双验证）；进行中：三方编辑器模态泵（全选 ours → Resolve 提交）、冲突块选择（多块 1/2→2/2 部分解决状态机）、滚动同步回归。**测试基建 bug 修复（探针实证）**：CreateConflictMulti 的块间上下文仅 1 行，git 合并（marker size=7）把相邻变更并进同一冲突 hunk → 实际 1 块而非 2 块，"0/2" 断言失败；已改为 7 行 sep 分隔（探针复测 2 块）。**模态泵死锁防护**：handler 内断言失败 → 窗口滞留 → DispatcherFrame 永不退出 → dotnet test 无限挂起（CPU 0% 实证）——catch 必须强制 Close(false) 让错误以测试失败形式浮出（已给全部 3 个模态用例加防护）。**git 语义教训（探针实证）**：全选 ours 的解析内容 = HEAD → checkout-index stage=2 + add 后 porcelain 无任何条目（无差异不显示），文件从两个列表都消失而非"移入已暂存"——验证暂存迁移必须选 theirs（stage=3 ≠ HEAD），两条路径互补（用例 2 改 theirs、用例 3 覆盖 ours 无条目语义）。
- 2026-09-05：模块 10 完成（五用例全绿，截图 10-mergeconflict/ 12 张，**首启用 1920×1280 最大化截图口径**）：① ResolveButton 三态状态机（Merge/Choose {ours}/Choose {theirs}/禁用往返）；② Choose theirs 主入口（ResolveConflictGitCommand → checkout-index stage=3 + add，UI 暂存迁移 + git 双验证）；③ 三方编辑器模态泵（三编辑器装配、0/1→1/1、全选 ours 提交 → 内容=HEAD 无状态条目、文件双列表消失）；④ 冲突块选择（行级 OnMergeLineAdded 按块 1/2→2/2，block1 ours + block2 theirs 混合落盘 "M " 暂存）；⑤ 滚动同步回归 + 冲突块导航 + 布局方向切换（Vertical↔Horizontal Merged 编辑器 ColumnSpan 迁移）。**两项测试基建修复（本轮根因闭环）**：a) `UiClick.Toggle` 升级为生产点击序（赋 IsChecked + raise Click）——MergeConflictUserControl 的 Local/Remote CheckBox 与 WPF 原仓同为 `Click="MergeCheckBox_Changed"` 绑定（ForkPlus 原仓 MergeConflictUserControl.xaml 247/282 行实证），仅赋 IsChecked 不触发 Click 处理器；补发 Click 对 IsCheckedChanged 绑定的 CheckBox（SideBySideMergeWindow 全选框）是无害空操作。b) 用例 2 断言改 TrFormat("Choose {0}") 全量等值——本地化字典只有格式键 "Choose {0}"，裸键 Tr("Choose") 回退英文原文，中文文案不含它（首跑实证：按钮已是"选择 theirs"却断言失败，纯断言口径错误）。**Next/Prev 导航语义澄清（探针 + 原仓对照，非迁移回归）**：ScrollToLine 走 AvaloniaEdit `ScrollTo(line,-1)` → `MinimumScrollFraction=0.3` 守卫，目标与当前偏移差 < 0.3×视口高时抑制滚动；首块在文件顶部时从顶点 Next 差 ~52px 被抑制且中线行不动（原版同款行为）——测试模拟"用户已聚焦首块"（compat 直设偏移把首块行滚到中线）后 Next→第二块（大位移过守卫）→Prev→回首块，断言稳健（中线落行 11-13 的容差内 FindNextChunk 均跳过首块）。**基建新增**：ScreenshotHelper.Snap 内置"1920×1280 放大 → 截帧 → 复原尺寸+全部 ScrollViewer 偏移"（用户截图口径约定），全量回归实证模块 1-9 不受影响；行高估算口径 = extent 高/行数（探针实证与 ScrollToLine 中心定位吻合，误差 <0.3%）。
- 2026-09-05：模块 11 完成（E2e11BranchOperationsTests 十用例全绿，截图 11-branchops/ 13 张）：① 创建分支（空名/重复名/非法名三态禁用 + checkout 开关命令预览 `branch`↔`checkout -b` 切换 + 真实建支切支）；② 检出分支（命令预览 + 真实切支 + 工作区文件迁移）；③ 重命名本地分支（预填当前名/同名/重复名禁用 + `-m` 预览 + 真实改名）；④ 删除本地分支（非活跃 `-D` 预览 + 真实删除）；⑤ 删除远程分支单/多两模式（单分支 GitPointView 视图 + 多分支 GitPoints 列表装配，`push --delete` 真实删远程，ls-remote 直查 bare 验证）；⑥ 跟踪远程分支（预填 ShortName + `checkout -b` 跟踪预览 + 真实建本地跟踪分支（`branch.<name>.remote` 配置验证））；⑦ 多分支推送（列表装配含 "(new)" 上游名 + `--set-upstream --atomic` 真实推送）；⑧ worktree 检出（默认路径 `<repo>-worktrees/<branch>` 预填 + `worktree add` 真实创建 + 新 tab 开启收尾关闭）；⑨ Lean 分支流程（Start 建支切支含空名/重复名校验 → 单提交后 Finish 合回 main fast-forward，含 `You must sync` 系列前置校验的同步对齐）。**三项测试口径修正（非生产 bug，按 bug 修复策略原仓对照判定）**：a) 非法分支名用例以 `:` 为准——两仓 `ReferenceNameValidator` 规则集相同（`HasIllegalCharacters` 只拦 `?`/`*`/`[`，控制字符拦 `~`/`^`/`:`），空格虽被 git 本体拒绝但两仓校验器均不拦（原始行为，非迁移回归）；b) RemoveRemoteBranch 单分支模式断言 `GitPointView.Value`（构造器按数量分流：单=视图、多=列表，`GitPoints.ItemsSource` 单模式本就不装配）；c) PushMultipleBranches `OnSubmit` 是"入队即关"模式（`JobQueue.Add` 后立即 `Close()`，与 AddUndoable 型"命令完成才关"不同）——终态断言须轮询 `ls-remote` 而非弹窗关闭即查（shell 手工复现 push 命令成功排除生产嫌疑后定位为时序口径）。**截图口径补充（宽度封顶窗口）**：`SizeToContent=WidthAndHeight` 的 `CheckoutBranchWindow`（`MaxWidth=670`，WPF 原仓同款）内容驱动宽回缩自然宽，Snap 临时降为 `Height` 后按窗口允许的最大宽 670 渲染（其余 12 张：PushMultipleBranchesWindow 无 SizeToContent 全量 1920×1280；SizeToContent=Height 型弹窗 1920×自然高）。**本地化口径**：CreateBranchWindow 重复名警告是全串拼接直传 `SetStatus`，Translate 内置 TranslatePattern 格式键模式匹配回退命中 `Branch '{0}' already exists` → zh-Hans 输出，按 `TrFormat` 断言（与 LeanBranchingStart/RenameLocal/TrackRemote 直接 `string.Format(Translate(fmt))` 同效）。
- 2026-09-05：模块 12 完成（E2e12TagOperationsTests 七用例全绿，截图 12-tagops/ 10 张）：① 创建标签（空名/重复名（`Tag '{0}' already exists` 格式键回退，PreferencesLocalization 显式 ReplacePattern）/非法名 `:` 三态禁用 + 无消息 `git tag -a <name> <commit>` ↔ 有消息 `-m "<msg>"` 加引号预览 + 真实建附注标签（`cat-file -t`=tag、`%(contents)` 消息、`^{}` 指向 main HEAD 三重验证））；② 创建并推送（推送开关预览追加 `git push <remote> refs/tags/<name>` 行 + 本地/远程双端验证 + 既有远程标签不动）；③ 删除单标签（单模式 GitPointView 装配 + 本地 `tag -d` ↔ 勾选"从远程删除"追加 `push --delete` 两态预览 + 本地/远程（ls-remote 直查 bare）双端删除）；④ 删除多标签（列表模式 GitPoints 装配 + `tag -d a b` 批删）；⑤ 单标签推送（ComboBox 自动选中 origin（upstream 推导）+ `push origin refs/tags/rel-3` 真实推送 + 未选标签不上远程）；⑥ 多标签推送（名字列表装配 + 单命令两 refspec 推送）；⑦ 标签详情（附注 + 轻量两形态）。**TagDetailsWindow 语义判定（探针实证，非迁移回归）**：tagger 三字段空、消息区=tagger 行+空行+tag 消息全文（附注）或提交消息（轻量）是两仓一致的原始行为——`bt_get_tag_details` 只接受 tag 对象 oid（探针三种输入直测：tag对象sha 成功返回结构化 tagger，剥壳 sha/轻量 sha 报 NotFound），而侧栏/提交列表的 Tag 由 `RepositoryReferences.New` 装配、其 dereferencedShaString 恒传同一个（剥壳）sha（两仓 `RepositoryReferences.cs`/`Reference.cs` 逐字节 diff 一致）→ `TargetObjectSha` 永远=剥壳 sha → if 分支侧栏链路两仓均不可达，恒走 for-each-ref 回退；且 Windows 原版 `bt_get_references` 也必返回剥壳 sha（否则 `ReferencesBySha` 键不上 commit sha、附注标签挂不到提交图节点，Fork 核心功能不可坏）。测试期望值用与生产完全相同的 for-each-ref 命令计算（`GitPsi` 与 `GitRequest` 同为单 Arguments 串 → 同一 .NET argv 解析 → 输出逐字节一致；.NET 按剥引号规则解析 `--format="..."`，与探针输出无引号互证）。**时序口径沿用**：CreateTag/RemoveTag 是"命令完成才关"（SubmitAndWaitClose），PushTag/PushMultipleTags 是"入队即关"（关窗 ≠ 推送完成，轮询 ls-remote——模块 11 教训）。**设置污染防护**：`CreateTag_Push` 在 OnSubmit 落盘，创建类用例 finally 快照恢复 + Save（模块 7 持久化教训）。
- 2026-09-05：模块 13 完成（E2e13HistoryRewritingTests 九用例全绿，截图 13-historyrewrite/ 16 张）：① 合并（干净：merge-tree 构造期预检 "without conflicts" + Fast-forward/No Fast-Forward 命令预览切换 + 真实 `--no-ff` 合并（合并提交双亲 + 6 主题 + 5 文件工作区）；冲突预检：警告图标 + "will cause conflicts" + 取消路径仓库零变更）；② 变基（构造期预检 + `git rebase refs/heads/main`（Reference.ObjectName=FullReference 口径）+ autostash 开关预览往返 + 真实变基：3 提交重放线性化 + merge-base=main + 文件迁移）；③ 拣选单提交（单视图折叠 + `--no-commit`/`-x` 选项预览 + no-commit 时 -x 开关禁用 + 真实执行：仅 f2.txt 落地）；④ 拣选多提交（列表模式 + 预览旧→新反转 + 两提交顺序重放）；⑤ 还原（`--no-commit` 预览 + 真实 revert：`Revert "base two"` 顶提交 + b.txt 消失）；⑥ 重置（Soft/Mixed/Hard 三态预览（SelectedIndex 0/1/2 生产 SelectionChanged 管线）+ 真实 hard reset：main 移动 + b.txt 连工作区丢弃）；⑦ Reflog（条目装配 + `{0} entries loaded.` 状态栏 + Jump 模态确认泵（模块 5 同款 DispatcherFrame）→ 真实 reset --hard 到所选状态 + Refresh 后新 reflog 条目入列）；⑧ 交互式变基（**真实 RI 辅助进程全链路**：构造即启动真实 `git rebase -i`（sequence.editor=ForkPlus.RI）→ RI 经命名管道 IPC 回传 todo → GetRebaseTodoListCommand 解析装配 3 提交 → 行内 ComboBox 生产路径选 Drop → OnSubmit 写回 todo → RI 放行 → git 完成变基 → feature 线性重放 f1+f3（f2 丢弃）+ main 成祖先）。**生产 bug 修复（迁移回归，探针实证）**：ResetBranchWindow "Reset type" ComboBox 打开即空——axaml 中 Mixed `ComboBoxItem IsSelected="True"` 在 Avalonia 下于用户首次展开下拉前不生效（关闭态 SelectedIndex=-1、SelectedItem=null，而命令预览却默认 `--mixed`，用户所见与将执行的不一致；WPF ComboBox 加载即生成全部容器故原版正常，Avalonia 容器在下拉 Popup 内延迟物化）。探针三段实证：show 后 -1/null → 开下拉变 1（容器物化才处理 IsSelected）→ 关下拉保持 1。修复：构造器编程式 `ResetTypeCombobox.SelectedIndex = 1`（与 `_resetType` 默认值 Mixed 一致，沿用 CherryPick/Revert 窗口同款模式），全仓扫描确认仅此一处 ComboBoxItem 受影响（另一处 IsSelected=True 是 TabItem，TabControl 容器即时机化无此问题）。**基建新增**：TestRepoFactory.CreateHistoryRewrite（main/feature 分叉无重叠：干净合并/变基/拣选/还原/重置）+ CreateHistoryConflict（同行修改：merge-tree 冲突预检）+ 测试内 EnsureDotnetRootForRiHelper（沙箱 DOTNET_ROOT 未设时 RI apphost 解析不到 .NET 运行时——"You must install .NET"探针实证，从当前运行时目录上推三级注入进程环境，git→RI 继承）。**时序口径**：Merge/Rebase/CherryPick/Revert/Reset/InteractiveRebase 均为 AddUndoable 型"命令完成才关"（SubmitAndWaitClose 直接适用）；ReflogWindow 是 CustomWindow 且 Jump 不关窗（终态轮询 rev-parse；确认框走模块 5 同款模态泵）。**git 语义教训（探针实证）**：`git log` 默认按提交日期序而非拓扑序——factory 中 main 的 base two 晚于 feature 的 f3 提交，合并后主题序是 [merge, base two, f3, f2, f1, base one]，跨分支断言除"最新必在顶"外应按包含集而非位置断言。**交互式变基兜底**：失败收尾走反射调 StopRebaseInteractiveProcess("cancel") 停 RI/git 进程再 Dispose（绕过 OnClosing 确认框防模态泵挂死），防进程泄漏殃及后续用例。
- 2026-09-05：模块 14 完成（E2e14StashTests 七用例全绿，截图 14-stash/ 10 张）：① 保存 stash（空态裸 `git stash push` /消息 `-m "..."`/Stage new files `--include-untracked` 三态命令预览（TextChanged→RefreshCommandPreview + IsCheckedChanged 生产管线）+ 真实保存：跟踪改动与已暂存新文件全部入栈、工作区还原干净（`status --porcelain` 空断言））；② 部分保存（GetChangedFilesGitCommand 生产数据源构造入参 + 文件列表装配（filesToStash 预勾选、自然序）+ 行内 CheckBox 取消勾选→提交禁用/预览清空、重新勾选→恢复 + pathspec 预览（消息含空格加引号）+ 真实部分 stash：仅 a.txt 回滚、b.txt/c.txt 原样未动）；③ 应用 apply（`apply`↔`pop` 两态预览往返 + 警告图标联动 + 真实应用：s2.txt 内容恢复（--index 连暂存态一并恢复）+ 条目保留）；④ 应用 pop（警告图标 + 真实 pop：内容恢复 + 条目删除只剩 stash-one）；⑤ 删除单条（单模式 GitPointView 视图装配/列表容器折叠 + `drop stash@{1}` + 真实删除）；⑥ 删除多条（列表模式 + "Delete 2 stashes" 按钮文案 + 批量 drop 清空）；⑦ 重命名（预填当前消息/同值禁用提交（IsSubmitAllowed）+ `git stash rename stash@{0} "..."` 预览 + 真实重命名：新消息入栈、条目数不变、OutResultSha 与 stash@{0} sha 一致且 ≠ 旧 sha）。**生产 bug 修复（迁移回归，探针实证）**：CreatePartialStashWindow 取消全部勾选后提交按钮仍启用、命令预览仍显示旧文件集——WPF 原版 `Checked/Unchecked` 事件触发时 TwoWay 绑定已把 IsChecked 写回 `ViewModel.Selected`（直接读 VM 是新值）；Avalonia 的 `IsCheckedChanged` 先于绑定回写触发，`FileCheckBox_Changed` 从 VM 读到旧值且此后无刷新机制（VM 是无通知的纯属性），用户在"全部取消勾选"状态下点提交会 stash 错误的文件集合（OnSubmit 读 VM）。探针实证：`myHandlerFired=True vm.Selected=False btn=True preview=[git stash push -- a.txt]`（VM 已更新但 UI 陈旧）。修复：处理器内直接读 `CheckBox.IsChecked` 同步推回 VM，消除对绑定回写时序的依赖。**git 语义教训（探针实证）**：`git stash push -m "msg"` 的 subject 是标准格式 `On {branch}: {msg}`（UI 侧 StashRevision.Message 解析时剥前缀、裸 `git stash list --format=%s` 带前缀）；而 `git stash store -m`（重命名执行路径 RenameStashGitCommand 的 commit-tree + store + drop 复合序列）存入的 subject 为裸消息不带前缀——两类断言分开口径。**基建新增**：TestRepoFactory.CreateStashWork（已提交 a/b 两文件，工作区：修改 a + 修改 b + 未跟踪 c——全量保存与部分保存共用基底）。**时序口径**：本模块 5 弹窗 OnSubmit 全部为 JobQueue/AddUndoable 型"命令完成才关"（SubmitAndWaitClose 直接适用，与模块 13 同款）；stash 条目对象取自 `repoControl.RepositoryData.Stashes`（生产侧栏同源管线装配，WaitForStashes 轮询到位后再建弹窗）。**设置污染防护**（模块 7/12 教训沿用）：SaveStash_StageNewFiles / ApplyStash_DeleteAfterApply 在 OnSubmit 落盘，用例开头快照 + 显式归零保证确定性初态，finally 恢复 + Save()。
- 2026-09-05：模块 15 完成（E2e15RemoteInteractionTests 七用例全绿，截图 15-remote/ 11 张）：① Fetch（origin 默认装配 + `--all` 开关预览/远程下拉禁用↔恢复往返（IsCheckedChanged 生产管线）+ 真实 fetch：origin/main 前进到远端 c2 而本地 main 原地不动、c2 的 b.txt 不落工作区——fetch 只动远程跟踪分支的语义断言）；② Pull（upstream 默认装配三要素（origin + refs/remotes/origin/main + 目标视图=活跃 main）+ `--rebase` 开关预览往返 + 真实 pull：behind 拉平（main 前进到 c2）+ b.txt 内容落地）；③ Push 已有上游（default 上游项装配（`default (origin/main)` 格式键本地化）+ track 复选框折叠（有上游时）+ `--force-with-lease` 预览与警告图标往返 + 真实 push：bare main 前进到本地 HEAD）；④ Push 新分支（无上游分支：track 复选框显示且默认勾选 + `new (origin/feature)` 项 + `--set-upstream` 预览往返 + 真实推送 + `branch.feature.remote/merge` 配置双验证）；⑤ EditRemote Add 模式（空名/重复名（`Remote 'origin' already exists` 全串直拼非翻译键，按原文断言）/合法新名三态 + `git remote add` 预览 + 真实添加：`remote get-url` 验证）；⑥ EditRemote Edit 模式（预填当前名/URL + 同值禁用（IsSubmitAllowed）+ set-url↔rename 双预览切换 + 真实 set-url：origin 指向 other.git）；⑦ AddCustomRefspec（构造入参预填 `origin/` 前缀 + 本地分支名 + OutRefspec 输出，PushWindow "Custom..." 子流程）。**轮询"将被创建的 ref"教训（首跑实证）**：Push 新分支终态是 bare 侧 refs/heads/feature 从无到有——轮询条件不能直接 `rev-parse` 该 ref（推送在途时非零退出经 GitOutput 抛 InvalidOperationException 炸出 WaitFor），须用 `ls-remote --heads origin`（恒 exit 0）+ 行匹配（模块 11 多分支推送同款口径）；rev-parse 只适合"已存在 ref 前进"（origin/main、bare main）的轮询。**偶发挂死观察（根因已定，模块 16 修复）**：首跑曾现 16 分钟 testhost 空转挂死（截图停在 07 后、无 git 进程、CPU 31%），隔离探针（剪贴板任务/PushFrame shim/EditRemote 构造器逐段标记）全部正常，触发源（上述轮询异常穿越 InvokeAsync 操作）修复后连三轮复跑未再复现——当时疑为冷启 JIT 下异常穿越 Dispatcher 操作的偶发死锁。**模块 16 回归轮同位置复现后 dotnet-stack + dotnet-dump 定论**（详见模块 16 条目）：真根因是前序用例的后台刷新任务对已删除临时仓库失败 → 排队模态 ErrorWindow 无人关闭 → 双层 PushFrame 死锁；已在 CloseRepositoryTab（JobQueue 排空）+ HeadlessAppBootstrap（错误弹窗看门狗）双层修复。**基建新增**：TestRepoFactory.CreateRemoteBehind（bare 远程 + work/other 双克隆交叉推送：other 推 c2 后 work 不 fetch——behind 1 状态，供 Fetch（拉平 origin/main）/Pull（拉平 main）真实执行；bare 路径 = <work 同级>/remote.git）。**时序口径**：Fetch/Pull/Push 均为"入队即关"（关窗 ≠ 命令完成，终态轮询——模块 11 教训沿用）；EditRemote 是 JobQueue 型"命令完成才关"（SubmitAndWaitClose 直接适用）。**设置污染防护**（模块 7/12/14 教训沿用）：Fetch_FetchAllRemotes / FetchAllTags / Pull_Rebase / Pull_StashAndReapply / Push_PushAllTags 均在各自 OnSubmit 落盘，用例开头快照 + 显式归零（污染值会让 Toggle(true) 因无变化不触发 IsCheckedChanged），finally 恢复 + Save()。**剪贴板共享状态注意**：EditRemote Add 构造器读进程剪贴板预填 URL（headless 下返回空、构造器安全），但模块间若有用例写剪贴板需留意此预填路径。
- 2026-09-05：模块 16 完成（E2e16SubmoduleWorktreeTests 六用例全绿，截图 16-submodule-worktree/ 6 张）：① AddSubmodule（双空/仅路径/齐备三态启停 + 无 URL（`git submodule add sub`）与含 URL 双预览形态 + FinalPathHint 绝对路径提示 + 真实添加：本地路径克隆 subsrc → sub、`.gitmodules` 记录 URL、`submodule status` 可查询）；② DeleteSubmodule（"指针前进 + 工作区脏"双状态子模块的 `deinit -f` + `rm -f` 双命令预览 + 真实删除：sub 目录移除 + `ls-files -- sub` 索引清空）；③ SubmoduleDiff 视图（Commit 视图选 sub → FileDiffControl 分发 SubmoduleDiffUserControl：`Submodule '{0}' changed` 标题 + 1↑ ahead 角标 + `1 uncommitted file` + s1..s2 修订列表装配 + 有脏文件时 Update 按钮隐藏）；④ CreateWorktree（空名/既有分支（`Branch '{0}' already exists`）/被 worktree 占用（`Worktree '{0}' already exists`，WorktreesByFullReference 拦截）三态警告 + 合法新名路径自动派生（容器/分支名）+ `worktree add` 预览 + 真实创建 `-b` 新分支 + 主窗口自动开新标签）；⑤ CheckoutBranchAsWorktree（占用分支禁用但路径/预览照常装配 + feature/two 真实检出既有分支（路径按分支名派生 `/`→`-`）+ 自动开新标签）；⑥ DeleteWorktree（remove 预览 + 真实删除：目录移除 + `worktree list` 不再列出 + 分支本身保留）。**生产 bug 修复（迁移后新 git 环境功能性回归，探针实证）**：git ≥ 2.38.1 出于安全默认禁止 file transport（protocol.file.allow=user），本地路径 URL 的 `submodule add`/`submodule update` 一律失败（"fatal: transport 'file' not allowed"，沙箱 git 2.50.1 实证）——而 AddSubmoduleWindow 明确支持本地路径 URL（剪贴板识别还专门检测本地目录）。修复：AddSubmoduleGitCommand（submodule add）与 UpdateSubmodulesGitCommand（update 全部调用点）命令前注入 `-c protocol.file.allow=always`（按用户显式输入本地路径的意图放开）。**测试基建双层修复（全量回归挂死根因，dotnet-stack + dotnet-dump 闭环实证）**：模块 16 完成后的全量回归空转挂死 15+ 分钟（CPU 0.1%，截图停在 15-remote/07 后），与模块 15 首跑"偶发挂死"同位置（E2e15 的 AddCustomRefspec——类内最后一个用例，非随机）。抓栈：UI 线程卡在两层嵌套 WindowDialogCompat.ShowDialog → PushFrame；堆转储读出两个 GitCommandError+BtError 文本：对已被 TestRepoFactory.Cleanup 删除的临时仓库（`/tmp/fpe2e_bare_*/work/.git`、`/tmp/fpe2e_remotebehind_*/work/.git`——模块 11/15 的用例）打开 bt 仓库失败。机制：后台刷新任务在 tab 关闭后仍在 JobQueue 跑 → 测试 finally 删目录 → 刷新失败 → RefreshRepositoryCommand 向 Dispatcher 排队模态 ErrorWindow.ShowDialog() → headless 无人点 Close → 双层 PushFrame 永久死锁。修复两层：a) **消除源头**——E2eMainWindowHarness.CloseRepositoryTab 关 tab 前按 GitModule.Path 抓取 RepositoryUserControl 引用，关闭后排空其后台 JobQueue（IsIdle 轮询 + RunJobs 泵 Post 回调，10s 上限），目录仍在时刷新成功不弹错；b) **看门狗兜底**——HeadlessAppBootstrap 启动 200ms DispatcherTimer（模态 PushFrame 泵消息期间也会触发），自动关闭任何可见 ErrorWindow 并记录文本；Run<T> 收尾同步扫描一次，动作期间出现错误弹窗 → 测试以根因文本失败（不再无限挂死），用例间遗留弹窗记控制台不归责。**基建新增**：TestRepoFactory.CreateSubmoduleSource（独立源仓 2 提交，供 add 克隆）/CreateSubmodule（parent + "指针前进 + 工作区脏"双状态子模块——sub 先退 s1 随 p2 提交（父仓记录 s1）再前进回 s2 + 改 s.txt 不提交；factory 自身 submodule add 同样需 protocol.file.allow）/CreateWithWorktree（parent + feature/one 已检出为链接 worktree，供占用校验/删除/检出）。**多目录形态清理口径**：CreateSubmodule/CreateWithWorktree 返回 parent 但 root 下有兄弟目录（subsrc/worktrees 容器），finally 须 Cleanup 整个 root 而非仅 parent。**时序口径**：本模块 5 弹窗均为"命令完成才关"（SubmitAndWaitClose 直接断言 GitResult，模块 15 EditRemote 同款）；CreateWorktree/CheckoutBranchAsWorktree 的 OnSubmit 成功后 TabManager.OpenRepository 自动开新标签——收尾须先关新标签再关父仓标签，防泄漏到后续用例。**git 语义教训（首跑实证）**：`rev-parse` 输出带尾部换行，拼进 pathspec 前必须 Trim（不 Trim 会炸 checkout）。
- 2026-09-06：模块 17 完成（E2e17WorkflowTests 九用例全绿，截图 17-workflows/ 12 张）：① GitFlowInit（预填（MainBranch() 检测 main 分支 + develop/三前缀固定默认）+ 空 master 警告禁用 + `git flow init` 预览 + 真实初始化：develop 分支创建 + gitflow config 全量写入（branch.master=main 探针式自定义保留——与 InitGitFlowGitCommand 在 MainBranch() 检测到 main 时的写入链路一致））；② GitFlowStartFeature（下拉装配（develop 选中）+ 前缀 TextBlock + 空名/重复名（`Branch '{0}' already exists` 格式键回退，TrFormat 断言）禁用 + `feature start f2 develop` 预览 + 真实 start 建支切支）；③ GitFlowFinishFeature（下拉只列 feature/* + `-r`/`--no-ff` 选项依序追加预览组合 + 真实 finish（-k 保留分支）：f1 提交合回 develop + HEAD 切 develop）；④ GitFlowRelease 全链路（Start（空名禁用 + `release start 1.0 develop` 预览 + 真实建支切支）→ release 分支提交 → Finish（TagMessage + -k + 真实 finish：tag 1.0 创建 + rel 提交合回 main 与 develop））；⑤ GitFlowHotfix 全链路（Start 选 main 起点（预览含起点名 `hotfix start h1 main`）→ 提交 → Finish：tag h1 + hfix 提交合回 main）；⑥ GitMmInit（三必填校验 + 预览（manifest/branch/group 置空回退默认 dependency.xml/master/default；-g 恒在预览（CreateInitArgs 固定 9 参数）+ 自定义值与空格 URL/group 引号转义）+ 目标目录非空拦截（MessageBox 弹出文本断言））；⑦ GitMm 检测与版本纯逻辑（IsGitMmWorkspace（.mm/.repo/普通/空串/null 五形态）+ FindAncestorGitMmWorkspace（子目录向上找最近工作区根）+ ParseVersion 多形态（带前缀/裸版本/两段/无数字）+ GitMmVersionChecker.Check 四态（NotFound（不存在/空）/Unsupported（假脚本 2.0.1）/Ok（3.2.0）/Unknown（无数字输出））+ GetGitMmVersionShellCommand 多行输出取首行）；⑧ GitMm 工作区 tab（.mm 目录经 TabManager.OpenRepository 分流建 GitMm tab：GitMmUserControl 挂载 + WorkspacePath/`git mm: ` 标题 + 无 git-mm CLI 时 WarnIfGitMmUnavailable 后台 Task 弹 ErrorWindow（ExpectErrorDialogs 断言 zh-Hans 全键译文））；⑨ LeanBranching Sync（守卫路径：activeBranch≠main 且 main 与上游**分叉** → `'{0}' is not in sync with '{1}'...` 错误弹窗 + sync 目录未创建 → 回置纯落后并切回 main 后真实 Sync：状态机步进（Step1 rebase 快进 main 到 origin/main + orig==localMain 跳 Step4/5 + sync 目录清理））。**环境依赖（探针实证）**：GitFlow 真实链路依赖 gitflow-avh（沙箱纯净 git 无 flow 子命令）——已源码安装到 ~/.local/bin 并写入 ~/.bashrc（跑测试须 PATH 含之；exec-path 方案无效：git 查找 git-flow 走 PATH 而非 exec-path）；命令链非交互性已探针：flow init -d 在 9 项 config 预置后不提问，feature/release/hotfix 的 start/finish 全部 exit=0。**GitMm 四命令窗口（Start/Reference/Sync/Upload）环境不可测**：构造依赖 GitMmWorkspaceItem 真实子仓扫描（git mm scan CLI），沙箱无 git-mm CLI——以 Init 窗口 + 检测/版本逻辑 + 工作区 tab 装配覆盖 GitMm 的非 CLI 面。**测试口径修正（四项，均非生产 bug——按 bug 修复策略逐项对照生产源码判定）**：a) **AreInSync 语义**（首跑守卫未触发根因）：BehindAheadCountExtensions.AreInSync 定义为"未分叉"——Left==0（纯落后）直接 true、Right==0（纯超前）true，唯 Left≠0 且 Right≠0（分叉）才 false；守卫测试前置须造分叉（fetch 后再在 main 造本地独有提交），纯落后/纯超前均不拦。b) **gitflow-avh finish 终态**：release/hotfix finish 的 CLI 链路最后一步是 checkout develop + merge → HEAD 停在 develop（非 main；ForkPlus 命令仅封装 CLI 无后续 checkout，终态由 CLI 决定）。c) **GitFlowSettings 合法 null**：未 init 的仓库 GetGitFlowSettingsGitCommand 返回 null（生产代码全量判 null），等待条件不能含 GitFlowSettings != null（首跑 CreateClean/CreateRemoteBehind 双双 15s 超时）；需配置的用例另调 WaitForGitFlowSettings。d) **GitMm 预览默认值持久化污染**（模块 7/12/14 教训再犯）：InitGitMmRepositoryWindow.RestoreDefaults 从 ForkPlusSettings.Default.GitMm 恢复 manifest/branch/group（SaveDefaults 提交时落盘）——测试须显式置空三字段走 IsNullOrWhiteSpace 回退默认，不依赖持久值。**基建新增**：a) HeadlessAppBootstrap.ExpectErrorDialogs/TakeCapturedErrorDialogs/PeekCapturedErrorDialogs（模块17 起：预期错误弹窗——Lean sync 守卫与 GitMm missing 警告的弹窗本身就是被测行为，看门狗照旧关闭+记录，Run 收尾不因捕获而失败，用例内轮询 Peek 等看门狗 200ms 滞后后取文本断言）；b) E2eMainWindowHarness.WaitForRepositoryJobs（入队型命令（LeanBranchingSyncCommand）真实执行后等 JobQueue 排空再断言仓库终态）；c) E2eMainWindowHarness.OpenTab（不断言 ActiveRepositoryUserControl——GitMm 工作区等 TabManager 分流场景）；d) TestRepoFactory.CreateClean（干净仓库供 Init）/CreateGitFlow（main + develop + 完整 gitflow config 预置 + flow init -d，供 Start/Finish 测试）。**模态弹窗关闭教训（GitMmInit 目标目录拦截）**：MessageBoxWindow 由 OnSubmit 同步路径弹出（未 await，弹后即返回），关闭任务不能 `closer.Wait()` 阻塞 UI 线程（InvokeAsync 无人泵永不执行 → 死锁至超时）——正确姿势：后台 Task.Delay 后 `Dispatcher.UIThread.Post` 关闭动作，测试线程用 UiClick.WaitFor（内含 RunJobs 泵）等事件置位。**本地化口径（沿用）**：GitFlowStartFeature 重复名警告全串拼接直传但 Translate 内置格式键回退命中（模块 11 同款）；GitMm missing 警告/ErrorWindow 文本断言用 TrFormat 全键等值（zh-Hans："未找到 git-mm 可执行文件 (git-mm.exe)。…"，裸英文子串断言会跨语言失败）。**生产 bug：本模块未发现**（六项失败均为测试口径问题，逐项修正后全绿）。
- 2026-09-06：模块 18 完成（E2e18LfsTests 六用例全绿：Track ✓ / Status ✓ / Fetch ✓ / Pull ✓ / Init+Deinit ✓ / Prune ✓；截图 18-lfs/ 6 张）。**① Track**（空模式提交禁用 + 空模式预览回退 "0 files match"（ls-files -i 无 --exclude 报 fatal 的失败路径，生产行为）+ 单模式提交启用/命令预览 `git lfs track *.dat` + 匹配文件预览（0.3s 防抖 + 后台 ls-files 真实执行 → "2 个文件匹配" + extra1/extra2 列表）+ 多行模式命令按行拼接 + 真实提交：`git lfs track *.dat *.bin` 写 .gitattributes（*.dat 追加 + *.bin 幂等不重复））；**已过五用例**：② Status（列表装配（git lfs ls-files）+ Lock/Unlock 真实链路（经测试基建 LfsLocksApiServer 本地 HTTP locks API——见下）+ Owner 显示/清空 + 重复 Lock 409 错误弹窗（ExpectErrorDialogs）+ FilterTextBox 过滤防抖清空/保留）；③ Fetch（origin 装配 + `git lfs fetch origin` 预览 + 真实 fetch：对象落 .git/lfs/objects 且工作区仍指针——fetch 不 smudge 语义）；④ Pull（预览 + 真实 pull：工作区 data.bin smudge 成真实内容（与工厂 Random(18) 同种子复算字节断言））；⑤ Init/Deinit（IsGitLfsInitializedGitCommand 纯文件判定三态（无钩子/含 `git lfs pre-push` 钩子/不含内容钩子）+ InitGitLfsCommand → `lfs install --local` 钩子落地 + DeinitializeGitLfsCommand → 钩子移除，经 JobQueue 生产管线）；⑥ Prune（GitLfsGetPreviewFilesGitCommand 匹配预览纯逻辑（*.dat 2 文件 / *.bin 含已提交 data.bin）+ GitLfsPruneCommand JobQueue 真实执行 + prune 后 ls-files 完好）。**Fetch/Pull 首跑失败根因（测试自身 bug，已修）**：LFS 对象路径布局是 `objects/<oid[0:2]>/<oid[2:4]>/<oid>`——第二级目录是 **2 字符**（如 bd/8d/bd8d...），用例误写 `oid.Substring(2, 4)`（4 字符目录 bd8d/），fetch/pull 的对象传输其实一直成功（诊断 dump 实证对象已落盘 size=2048），测试在错误路径下 File.Exists 而超时。修正为 `Substring(2, 2)` 后两用例即绿——生产链路（GitLfsFetchGitCommand/GitLfsPullGitCommand → GitRequest.ExecuteBt → git-lfs 3.0.2）无任何问题。**环境闭环（探针实证）**：① 沙箱 git-lfs 3.0.2 已装（/usr/bin/git-lfs），app 派生 git 经 PATH 找到（Lock/ls-files/fetch/pull 全链路实证）；② 本地 bare 远程的 LFS 对象传输闭环：源仓 pre-push 钩子（`lfs install --local` 装）在 push 时把对象复制进 bare 的 lfs/objects（远程 URL 须**绝对路径**——相对路径不触发传输）；克隆检出指针文件（无全局 lfs filter 配置时 clone 不 smudge），再 `install --local`（pull 的 smudge 需要；指针文件 clean 幂等，status 仍干净）；③ locks API：git lfs lock/locks/unlock 走 HTTP LFS API（读 lfs.url 配置），file:// 远程报 "missing protocol"——测试基建 LfsLocksApiServer（127.0.0.1 随机端口，GET /locks + POST /locks(201/409) + POST /locks/{id}/unlock(200/404) 三端点，Content-Type=application/vnd.git-lfs+json）实现；④ 空模式 Track 预览：`ls-files -i` 无 --exclude → git 报 fatal（exit 128）→ 命令失败回退 "0 files match"（生产行为，非 bug）。**基建新增**：TestRepoFactory.CreateLfs（lfs install --local + track *.bin + data.bin（Random(18) 2048B 确定性字节）+ normal.txt）/CreateLfsRemoteBehind（bare remote.git + src 源仓 + work 克隆三件套，返回 work；对象传输闭环见上，Cleanup 须清整个 root）/LfsBytes（工厂与用例同种子复算）；LfsLocksApiServer（如上，owner 固定 "Test User"——GetLfsLocksGitCommand 解析 `path\towner\tID:...` 三段 tab 输出）。**Track 用例失败根因闭环（框架源码级实锤，测试口径修正非生产 bug）**：`Track_ValidationPreviewAndRealTrack` 首版确定性挂在 "非空模式提交应启用"——`dialog.PatternTextBox.Text = "*.dat"` 赋值后**直接**断言，`footer.SubmitButton.IsEnabled` 仍 false。真因：Avalonia 12 的 `TextBox.RaiseTextChangeEvents()`（12.1.1 源码）里 **`TextChanged` 事件是异步派发**——`Dispatcher.UIThread.Post(…, DispatcherPriority.Normal)`，注释明确 "following WinUI"（`TextChanging` 才同步）。程序化 `.Text` 赋值后派发事件躺在调度队列里，不泵 `RunJobs()` 就读不到事件落地后的状态。此前记录的三个假设（Footer 重建/IsOperationInProgress 滞留/PlaceholderTextBox 怪癖）全部不成立：UpdateSubmitButton 直写唯一 footer 实例的按钮、GitLfsTrackWindow.IsSubmitAllowed 不含 InProgress 检查、TextChanged 照常触发只是异步。修正：两处 `.Text` 赋值后补 `Dispatcher.UIThread.RunJobs()`（模块 11/14/15/17 的既有口径——套件内所有 TextChanged 驱动的断言都如此，本用例首版漏泵是唯一例外）。生产链路无恙：真实应用有消息循环，派发事件必然在用户可感知前落地，与 WPF 同步事件最终效果等价（框架语义差异非迁移回归）。01-track-preview 截图已补（pattern *.dat + "2 个文件匹配" + extra1/extra2 列表 + 命令预览三要素齐备）。**git 语义备注**：git-lfs `ls-files` 输出 `<oid前7位> - <path>`；LFS 对象本地存储 `.git/lfs/objects/<oid[0:2]>/<oid[2:4]>/<oid>`（两层 2 字符目录——本模块教训）。
- 2026-09-06：模块 19 完成（E2e19PatchSnapshotTests 七用例全绿：SaveAsPatch 单修订 ✓ / SaveAsPatch 范围 ✓ / ExportPatch 命令直测 ✓ / ApplyPatch 文件路径 ✓ / ApplyPatch 剪贴板 am ✓ / SaveSnapshot ✓ / SnapshotRestore ✓；截图 19-patchsnapshot/ 7 张）。**生产 bug 修复两项（探针实锋试出，WPF→Avalonia 迁移期行为差异，均已拉原仓源码对照定责）**：A. **SaveAsPatchWindow revisions 列表永远装配不上（生产必现）**——WPF 原仓在 OnInitialized override 里 Task.Run 加载 revisions，WPF 的 Initialized 在构造完成后触发（字段已就绪）；Avalonia 12 的 Initialized 在 **TopLevel 基类构造链中**触发（栈实证：Window..ctor → TopLevel..ctor → PresentationSource..ctor → set_RootVisual → OnAttachedToVisualTreeCore → InitializeIfNeeded → OnInitialized），此刻派生类构造器字段全未赋值（探针日志 `_gitModule=NULL _src=000…0`）→ GitRequest 对 null 模块 NRE → await 重抛进 catch → ItemsSource 恒 null（弹窗打开后 revisions 列表空白）。修复：加载逻辑挪到构造器尾部私有方法 LoadRevisions()（InitializeComponent 之后调用，方法体与原版逐字节一致）。**FileHistoryWindow.OnInitialized 同类潜伏 bug 已确认**（_repositoryUserControl 同样未赋值 → repositoryData null 早退 → 文件历史窗口空载；本套件无该窗口用例所以从未暴露），属模块 20 范围届时修复。B. **ApplyPatchWindow 剪贴板模式 Create commit 勾选框打开后消失（生产必现）**——剪贴板构造器折叠 PathTextBox 后，Avalonia 12 的 TextBox 初始化异步派发一次空 TextChanged（模块 18 根因同源的 WinUI 式异步派发），handler 以空路径重算 _patchContainsCommitHeader（File.ReadAllText("") 失败→false）→ From 头补丁的勾选框被折叠，git am 路径永不可达（探针日志实证：ctor 后 IsVisible=True，RunJobs 后 False）。WPF 原仓剪贴板路径 Text 从未赋值不触发事件。修复：`_patchData != null`（剪贴板模式）时 PathTextBox_TextChanged 直接 return。**七用例覆盖**：① SaveAsPatch 单修订（Revision: 标签 + revisions 装配（GetRevisionsInRangeGitCommand 真实执行）+ 裸预览 `git format-patch <sha7>` + OnSubmit 降级路径（headless NoopStorageProvider=用户在系统保存对话框点取消：窗口关 + 零导出 + RecentPatchDirectory 不被改写）+ 含空格输出目录引号预览 `-o "..."`）；② SaveAsPatch 范围（Revisions: 标签 + f2~..f3 双修订 log 逆序装配 [f3, f2] + 预览方向 src..dst（新..旧——与 WPF 原仓逐字节一致的原版行为：预览是展示语义，实际执行 ExportPatch 用 dst~..src））；③ ExportPatch 命令直测（窗口 OnSubmit 内部调用的同一命令对象，GitModule 直构免开主窗（模块 18 Track 同款）：单修订 --max-count=1 单 patch（From 头 + Subject + diff）/ 范围 f2~..f3 双 patch 带序号）；④ ApplyPatch 文件路径（预填 + 非 From 头勾选框折叠 + 含空格路径引号预览 + apply --check 冲突检测双态（Success/Warning 状态条往返）+ 不存在路径提交禁用（预览仍显示完整命令——原版语义：非空路径即预览，存在性由提交禁用表达）+ 真实 apply --3way：文件落暂存区不建提交）；⑤ ApplyPatch 剪贴板（Location/Path/Browse 三件套折叠 + From 头勾选框显示 + `git apply`↔`git am` 预览切换（IsCheckedChanged 生产管线）+ 真实 am：补丁提交入历史 + 文件落地）；⑥ SaveSnapshot（预览三态（裸 push → -m 消息 → --include-untracked，TextChanged/IsCheckedChanged 生产管线）+ 真实快照：stash create+store 入栈 1 条（subject `On main: snap msg`——stash create <msg> 恒加 `On <branch>: ` 前缀，store 保持）且**工作区完整保留**（快照与模块 14 SaveStash 清空工作区的语义分野：stash create 只建悬空 commit 不动工作区/index）+ stageNewFiles=true 先暂存新文件再入栈（`A  c.txt` 状态保留）+ stash show 内容三文件（stash create 捕获 index 的已暂存新文件））；⑦ SnapshotRestore（SnapshotGitCommand 抓快照三字段（headSha/branch/stashSha，tracked 脏 → stashSha 非 null 但 untracked 不入快照——见下 git 语义②）→ 破坏现场（切 wreck 分支 + reset --hard 退一提交 + 删 a.txt）→ RestoreSnapshotGitCommand 全恢复（checkout main + reset --hard headSha + stash apply --index：a/b 内容全回来 + 分支/HEAD 归位 + stash list 仍空（create 不入栈的悬空 commit 语义）））。**git 语义教训（三项，探针实证）**：① `git apply --3way` **隐含 --index**（git 文档原文 "--3way implies the --index option"，WPF 原仓同旗标）——新文件补丁落地后入暂存区（porcelain `A `）而非未跟踪（`??`），且不建提交；② `git stash create` 不支持 --include-untracked（**被当 message 吞掉且 exit 0**，subject 变 `On main: --include-untracked`，回退分支永不执行）——SnapshotGitCommand.ReadStashCreate 的 untracked 捕获实际不生效，untracked 不入快照；WPF 原仓逐字节同款原始行为（原版语义，按实际行为断言并注明，非迁移回归）；③ GitOf 助手的全局 .Trim() 会吃掉 porcelain **首行**前导空格（` M a.txt` → `M a.txt`）——XY 状态码断言必须逐行 Split 后比较（次行起不受影响）。**测试基建口径**：SnapshotRestore 命令直测须传真实 JobMonitor（`new JobMonitor()`——GitRequest.Execute 内部 JobMonitorExtensions.Append 无空守卫，null 会 NRE；生产链路 undo 执行恒走 JobQueue 带 monitor，测试同款直构才贴近入口）；SaveAsPatch 的 OnSubmit 同步直返（无命令入队）不走 SubmitAndWaitClose，ApplyPatch/SaveSnapshot 为 JobQueue 型"命令完成才关"（模块 11-18 口径沿用）。**设置污染防护**（模块 7/12/14/17 教训沿用）：RecentPatchDirectory（SaveAsPatch 预览与 OnSubmit 读写）/SaveStash_StageNewFiles（SaveSnapshot 复选框初始态）用例开头快照+显式归零，finally 恢复+Save()。**全量回归**：4231 用例 4230 绿（7m28s）——唯一失败 E2e10 SideBySideMergeWindow_ScrollSync_AndChunkNavigation_Regression 为全量高负载下既有偶发（单跑即绿；改动面仅 SaveAsPatchWindow/ApplyPatchWindow 两文件且仅模块 19 引用，与模块 10 零关联）。
- 2026-09-06：模块 20 完成（E2e20ViewerWindowsTests 七用例全绿：Blame 多提交 ✓ / FileHistory File 模式 ✓ / FileHistory Directory 模式 ✓ / RepositoryOverview 树图 ✓ / RepositoryStatistics 统计 ✓ / RevisionDetails 独立窗 ✓ / GoToLine ✓；截图 20-viewerwindows/ 8 张）。**生产 bug 修复一项（模块19 已探针定责、本模块修复落地）**：FileHistoryWindow.OnInitialized——WPF 原仓在 OnInitialized override 里加载文件历史（WPF 的 Initialized 在构造完成后触发，字段已就绪）；Avalonia 12 的 Initialized 在 TopLevel 基类构造链中触发，此刻派生类构造器字段全未赋值（_repositoryUserControl=null → 第一行 NRE → catch 吞掉 → 历史列表/时间线/diff 永不装配，生产必现空窗口）。修复：加载逻辑挪到构造器尾部私有方法 LoadHistory()（方法体与原版逐字节一致，仅去掉 base.OnInitialized() 调用）。**七用例覆盖**：① Blame 多提交（CreateHistoryRewrite(checkoutFeature:true) 的 f1.txt 两版本 blame：修订下拉装配 + RevisionTimeLine + blame 块双归属断言（context 行归旧提交、Added 行归当前提交——GetRevisionFileChangesGitCommand(showEntireFile:true) 实为 --unified=100000 超上下文 diff 而非全文件 Added，blame 视图保持正常 diff 结构）+ 修订切换导航（切到首提交 → 全行归首提交）+ Undo/Redo 栈生命周期）；② FileHistory File 模式（Mode.File 构造 → GetFileHistoryGitCommand 历史列表装配 + 首项自动选中 → 防抖 RefreshDiff → FileDiffControl.Content 装配 + Escape 关闭）；③ FileHistory Directory 模式（Mode.Directory → FolderHistoryEntryViewModel 文件夹条目 + 子文件 SubItem 条目 + ExpandAllChildren 展开全树）；④ RepositoryOverview 树图（JobQueue("Read repository overview") → GetRepositoryOverviewDataGitCommand → DateRange 赋值 → RefreshData → Treemap.DataSource + FirstVisualItem 叶子选中 → 联动提交列表/作者列表/选中文件名 + 主动切换 SelectedIndexPath=[0,1] 二级索引路径联动（单元素 [1] 越根数组界被 SelectionChanged try/catch 吞掉，文件名不更新——探针实证））；⑤ RepositoryStatistics 统计（GetRepositoryStatsGitCommand 硬门槛：>2 修订才出统计（<=2 → "no changes" → Fallback），须 checkoutFeature 的 4 提交分支；AuthorStatListBox 作者聚合（Test/4 提交）+ OxyPlot 四图状态断言（LineSeries 单作者/饼图单切片值 4/周几柱图恒 7 项/小时柱图恒 24 项零值占位/热图按日聚合值和=4））；⑥ RevisionDetails 独立窗（RevisionDiffTarget.Revision + 文件名构造 → Loaded → ShowRevisionDetails → RevisionDetailsUpdated → 标题 "sha7 subject" + Escape 关闭）；⑦ GoToLine（OnSubmit 行号解析纯逻辑：空/非数字 → null、有效数字 → 值、边界 0 → 值）。**OxyPlot.Avalonia × Avalonia 12 兼容闭环（运行时实锤，模块20 首个 PlotView 布局用例暴露）**：官方 oxyplot-avalonia 按 Avalonia 11.0.0 编译的 XAML IL 调用 TemplateBinding.ProvideValue()（11 时代签名），运行时 12.1.1 无此方法 → 任何 PlotView 进入布局/模板实例化即 MissingMethodException（布局期异常穿透渲染循环，应用级崩溃）。修复：仓库内置补丁 docs/patches/oxyplot-avalonia-avalonia12.patch（4 处：Directory.Build.props AvaloniaVersion 11.0.0→12.1.1；csproj TFM netstandard2.0→net10.0 + AvaloniaUseCompiledBindingsByDefault=false；PlotBase.cs 加 using Avalonia.Input.Platform（SetTextAsync 在 12 变 ClipboardExtensions 扩展方法）），CI（build.yml）在 clone 后显式 apply，MIGRATION.md 同步更新沙盒重装步骤。**2026-09-03 的"按 11 编译并存正常"旧结论作废**——当时只验证了编译通过（无任何用例真正渲染过图表）。注意 netstandard2.0 TFM 下直接升 AvaloniaVersion 会 786 个类型解析错误（Avalonia 12 无 netstandard2.0 资产），必须同步改 TFM。**探针实证（六项）**：① GetRevisionFileChangesGitCommand(showEntireFile:true) = --unified=100000 + --inter-hunk-context=100000 超上下文 diff，非全文件 Added；② git blame HEAD~ 对"父版本不存在的文件"返回 "fatal: no such path" 且 GetBlameGitCommand 捕获 stderr 返回 Success(空 chunks)——单提交文件 blame 照常装配（全 Added 归当前提交）；③ BlameWindow/FileHistoryWindow 加载链为 new Task(...).Start() + Dispatcher.Post（非 JobQueue 型），UiClick.WaitFor 内部泵可直接等待；④ blame 块头部过滤必须精确类型匹配 GetType()==typeof(BlameItemViewModel)（BlameItemBodyViewModel 是子类）；⑤ RepositoryOverview 等待锚点用 Fallback.IsVisible==false（初始 Show、数据到达 Hide）；⑥ RepositoryStatisticsWindow/RevisionDetailsWindow 装配入口都是 base.Loaded 事件——headless 下 Show()+RunJobs 会触发 Avalonia 的 Loaded。**全量回归**：模块20 改动面（FileHistoryWindow 迁移修复 + 测试新增 + OxyPlot 补丁与 CI/文档），单独套件 7/7 绿。
- 2026-09-06：模块 21 完成（E2e21PreferencesTests 九用例全绿：General 结构与开关 ✓ / General 语言切换 ✓ / Commit 校验 ✓ / Git 实例与身份 ✓ / Integration Shell 与保存 ✓ / CustomCommands 添加保存往返 ✓ / CustomCommands 删除确认 ✓ / AI Review 归一化与技能 ✓ / ImportExport 取消路径 ✓；截图 21-preferences/ 8 张）。**模式**：PreferencesWindow 直构（生产入口 ShowPreferencesWindowCommand 即 new + ShowDialog，构造器内 Initialize() 全量初始化 7 个 tab——所有控件经 x:Name 字段可达，无需切 tab 即可断言；切 tab 仅为了截图与"内容进视觉树"（AI 技能输入框是代码构建的私有 TextBox，经 CustomSkillInputBorder 视觉子树定位））。**九用例覆盖**：① General 结构与开关（7 tab 结构 + 本地化标题 + 开关生产点击序 → ForkPlusSettings 写回 + RevisionSortOrder 单选（Click 绑定 → 赋 IsChecked + 补发 Click，模块 10 Toggle 教训的 RadioButton 展开）+ Space 字符 ComboBox 装配与写回 + 字号边界 99→40 夹顶/3→10 夹底）；② General 语言切换（ComboBox 装配 + 切换重本地化：7 tab 标题 + footer 按钮 + 应用语言缓存 _appliedLanguage 刷新）；③ Commit 校验（拼写检查 ComboBox 三态（Disable/System/English）+ 长度指示/分页线/正则 TextChanged 解析与非数字回退（72 默认）+ 提交消息正则写回）；④ Git 实例与身份（**环境自适应断言**：设了 forkgitinstance env var（CI 兜底注入场景）→ ENV 项选中且下拉禁用；沙箱装有内置 git（App.EnvironmentGitInstancePath == null，诊断实证 /root/.local/share/ForkPlus/gitInstance/2.50.1/bin/git）→ Fork 内置实例 Local 选中 + git-mm/git-ai 空环境装配（仅分隔符 + 添加自定义项）+ 全局身份 TextBox → LostFocus → SetGlobalUserIdentityGitCommand 真实写 git config --global + AI 归属/verbose 开关写回）；⑤ Integration（Shell ComboBox 5 项装配（Default/WindowsTerminal/CommandPrompt/PowerShell/Custom）+ 切换联动路径/参数框 + ShowBugtrackerLinks + OnSubmit → Save 全链路）；⑥ CustomCommands 添加保存往返（添加菜单（修订/仓库两类 MenuItem Click 路由事件）+ 自动命名计数（Custom Command/1/2——CreateCustomCommandName 按既有前缀计数）+ Target 切换联动（Branch 目标引用容器显示）+ Submit 落盘 custom-commands.json（按名排序）+ 二次构造重载（持久化往返））；⑦ CustomCommands 删除确认（模态 MessageBox 确认泵（模块 5/13 同款 Dispatcher.Post(Background) 内找窗点按钮）：确认删选中项/取消留 + 全局模式 Location 容器折叠）；⑧ AI Review（ServiceUrl 归一化（/v1|/v1/chat/completions|尾斜杠剥离）+ 重试/超时解析回退（abc→回退值）与下限（5→10）+ 自定义技能添加（显式名）/同名更新/自动命名/删除 + 行号联动（多行内容 → 1\n2\n 行号块）+ AiDevSkillList JSON 持久化）；⑨ ImportExport（headless 存储提供器 Noop → 导出/导入双取消路径：无 zip 产生、无确认框、无重启、状态条不变）。**设置污染防护升级（本轮核心教训，模块 7/12/14/17/19 名单式快照的根治方案）**：偏好设置的每个 tab 都直写设置且多处处理器自带 Save()（UndoRedo/AiAttribution/GitInstance 选择等），按名单快照漏一个属性就**跨进程**污染——实证：CodeEditorFontSize 被用例改成 10 后经 Save() 落盘 settings.json，首轮全绿、次轮同用例初值断言失败（新进程从磁盘加载残留值）。修复：SnapshotPrefs 改为**全属性反射快照**（typeof(ForkPlusSettings).GetProperties 全量捕获可写属性，finally 逐属性还原 + Save()），名单参数废除。**另两项测试基建修正**：a) **SetGlobalUserIdentity 串行化**——处理器是 async void（Task.Run 内跑 git config --global），两个 LostFocus 背靠背触发会并发写 .gitconfig 触发 "could not lock config file" 错误弹窗（看门狗捕获根因文本），须等第一次落盘完成（轮询 git config --global --get）再发第二个；b) **git config 恢复助手**（TryGetGlobalGitConfig/RestoreGlobalGitConfig：--get 非零退出即未设置、恢复先 --unset 再按需写回，处理 null 语义）。**模块内幂等验证**：连续三轮 dotnet test（跨进程）9/9 全绿 + settings.json/custom-commands.json 终态干净（CodeEditorFontSize=13、custom-commands.json 不存在）。**生产 bug：本模块未发现**（Git 实例下拉的"选中项类型"首跑失败为环境差异非生产缺陷——ENV 探测链行为正确，断言口径修正为环境自适应）。
- 2026-09-06：模块 22 完成（E2e22RepositorySettingsTests 五用例全绿：General 身份与主分支 ✓ / CommitTemplate 模板落盘 ✓ / IssueTracker 双级开关 ✓ / IssueTracker 规则 CRUD ✓ / CustomCommands 本地模式 ✓；截图 22-repository-settings/ 5 张）。**模式**：RepositorySettingsWindow 直构（生产入口即 new RepositorySettingsWindow(module, repositoryData).ShowDialog()，构造器内 Initialize() 全量初始化 4 个 tab），但 GeneralUserControl.Refresh 读 MainWindow.ActiveRepositoryUserControl.RepositoryData（静态）——须先经 E2eMainWindowHarness.OpenRepository 真实开仓。保存路径走 footer SubmitButton → OnSubmit → 四控件 Save()。**五用例覆盖**：① General 身份与主分支（**本地身份默认态**：TestRepoFactory.Init 恒写 user.name=Test → 不勾全局 + 输入框启用 + 预填；勾选全局 → SetRepositoryLocalUserIdentity(null) → git config --local --unset 真实执行 + Refresh 全局模式（勾选+禁用）；取消勾选启用编辑 → 输入新身份 → Submit 关窗 → config 真实落盘 + 重开往返 + **新 GitModule 磁盘重载断言**（同实例 Settings 是惰性缓存，同实例断言只证内存）+ MainBranch 下拉（默认项 develop/main/master 探测 + "default (main)" 本地化标题 + 选择 feature → LeanBranchingMainBranch）+ NoFastForward 开关 + TabWidth 解析与非数字回退保留旧值）；② CommitTemplate（模板全局默认态（无 commit.template → 不勾本地 + 启用编辑）→ 取消勾选输入内容 → Submit → **commit_msg_template.txt 写 CommonGitDir + config commit.template=绝对路径**（SetLocalCommitTemplateGitCommand 的 Path.Combine(gitModule.CommonGitDir,...)——首版断言相对路径实证失败后修正）+ 重开内容往返 + 重勾全局 → config --unset（文件保留）+ SignOff/SkipCommitMessage 开关 + CommitMessageRegex 写回）；③ IssueTracker 双级开关（应用级 ShowBugtrackerLinks=false → 整 tab fallback "Issue Tracker Integration is disabled"；仓库级禁用 → 内容 fallback "Issue tracker integration for '{repo}' is disabled"（FallbackTitle=null FallbackMessage 断言）；勾选启用 → ShowContent() 移除 overlay）；④ IssueTracker 规则 CRUD（Sample GitHub 规则添加（MenuItem）+ 字段装配 + regex valid/invalid 双态（红边/watermark）+ Level 单选 Local→.git/issuetracker / Shared→.issuetracker（位置文本联动）+ Submit → .issuetracker INI 落盘（[issuetracker "name"] + regex/url 行，反斜杠转义翻倍）+ 重开往返 + 删除确认泵）；⑤ CustomCommands 本地模式（Location 容器显示（全局模式折叠的对照）+ 添加修订/仓库两类命令 + 自动命名计数 + Submit → .git/fork/custom-commands.json + Shared 切换 → .fork/custom-commands.json + OS 下拉显示）。**探针实证（五项）**：① ContentContainer.ShowFallback 是**叠加式**（原 XAML 内容 Grid 不移除，fallback 作为第二个子控件覆盖其上）→ 查找 fallback 必须按 FallbackTitle/FallbackMessage 过滤（ItemFallbackUserControl 恒为视觉后代，FirstOrDefault 裸取会拿错实例）；② Tab 容器是 ModernTabControl 且无 x:Name（唯一命名 TabItem 是 CustomCommandsTab）→ 经 GetVisualDescendants().OfType<ModernTabControl>() 定位，TabItem.Header 经 ForkPlusDialogWindow_Loaded 自动本地化（PreferencesLocalization.Apply 递归覆盖 Header/Content/Title）→ 按 Tr 断言；③ GitModule 构造器四参 (path, commonGitDir, worktreeGitDir, parentRepoPath)——单参不可用；④ Quotify("") 产出带引号空串仍是写值参数（空串身份写本地成功）；⑤ 本地自定义命令 SetLocalCustomCommands → 非共享 <CommonGitDir>/fork/custom-commands.json、共享 <repo>/.fork/custom-commands.json（按名排序后落盘）。**设置污染防护**：仅 IssueTracker 用例改应用级 ShowBugtrackerLinks（模块 21 全属性反射快照沿用）；仓库级设置随临时仓库销毁，无需额外防护。**全量回归**：4252 用例 4251 绿（8m33s）——唯一失败 InteractiveRebaseScreenshotTests.InteractiveRebase_FullFeatures_ScreenshotEvidence 为像素断言既有偶发（"ComboBox 选中框内黄色像素应>2（实际=0）"渲染时序，单跑即绿；与模块 22 改动面零关联）。**生产 bug：本模块未发现**。
- 2026-09-06：模块 23 **进行中**（积分耗尽，交接给下个 agent——**已实证 + 待查清单见下**）。**已完成**：① 生产 bug 修复一项（跨平台，探针实证）：GenerateSshKeyShellCommand/ValidateSshKeyShellCommand 原硬拼 `<gitInstance>/usr/bin/ssh-keygen.exe`（Windows Git 布局），Unix 内置 git 实例布局（gitInstance/2.50.1/ 下仅 bin/git）下 SSH 密钥生成/验证全坏 → 新增 SystemEnvironment.GetSshKeygenPath() 候选链（usr/bin/ssh-keygen.exe → usr/bin/ssh-keygen → bin/ssh-keygen.exe → bin/ssh-keygen → 系统 PATH）+ FindOnPath 助手，两命令改用探测链；② E2e23SshEnvironmentTests.cs 六用例已编写完成可编译（1 空态 / 2 列表详情落盘 / 3 勾选验证与多密钥警告 / 4 生成密钥真实链路 / 5 Git 实例候选联动 / 6 Workspaces 增删改）；③ ~/.ssh 隔离基建（IsolateSshDir 备份/还原，保护宿主密钥）。**测试现状（实证）**：用例 1 ✓（首版断言修正：空态详情 TextBlock.Text 是 XAML 初始 **null** 而非 ""——SelectedIndex=0 在空列表不触发 SelectionChanged，RefreshDetails 未跑；Cosmetic 非生产 bug）；用例 2 ✓（列表装配/Sha256 指纹/Submit 落盘/重开往返全过）；**用例 3 ✗ 待查**：UiClick.Toggle(checkBox, true) 勾选后 SshConfigurationTextBlock 仍是 fallback 文本（期望 "e2e-tog-a" 实际 "默认系统 ssh-agent"）——已排查到 XAML 是 `IsCheckedChanged="SshKeyCheckBox_Changed"` 绑定（非 Click），**UiClick.Toggle 赋 IsChecked + 补发 Click 的事件序对 IsCheckedChanged 处理器无效**（IsCheckedChanged 在赋值瞬间同步触发，但处理器读 `(sender as CheckBox)?.Parent as DockPanel)?.DataContext`——怀疑 Toggle 时 CheckBox.Parent 尚未挂接或 DataContext 未装配，需在 Toggle 前加 RunJobs 等 DataTemplate 实例化完成，或直接查 CheckBoxOfKey 定位到的 CheckBox 的 Parent/DataContext 是否 null——用诊断断言定位）；用例 4/5/6 **未跑过**（首轮全量 dotnet test 挂起 15 分钟无输出后手工终止——无 testhost 卡死证据（CPU 0%），怀疑是轮次内某用例等待超时/看门狗弹窗未关，**下个 agent 请用 timeout + 单用例逐个跑**，勿直接全量）。**注意**：git status 里 docs/evidence/ 大量 png 是本轮全量回归截图刷新（内容等价重渲染），一并提交即可。
- 2026-09-06：模块 23 完成（E2e23SshEnvironmentTests 六用例全绿：空态 ✓ / 列表详情落盘 ✓ / 勾选验证与多密钥警告 ✓ / 生成密钥真实链路 ✓ / Git 实例候选联动 ✓ / Workspaces 增删改 ✓；截图 23-ssh-environment/ 5 张；连跑两轮 6/6 幂等绿）。**生产 bug 修复一项（迁移回归，模块 14 同款时序问题，WPF 原仓对照定责）**：ConfigureSshKeysWindow 勾选密钥后 SSH configuration 文本不联动且验证被跳过——WPF 原仓 XAML 是 `Checked/Unchecked` 双绑定（WPF 事件触发时 TwoWay 绑定已把 IsChecked 写回 VM.IsActive，处理器读 VM 即新值）；Avalonia 12 的 `IsCheckedChanged` 先于绑定回写触发，处理器读 VM.IsActive 得旧值 false → 跳过 ValidateSshKey → RefreshConfigutationTextBlock 输出 fallback "default system ssh-agent" → 此后绑定才回写 true 但无任何刷新链路（SshKeyViewModel 有 INPC 但窗口不订阅）。用户可见后果：勾选密钥后配置文本恒显示 default system ssh-agent、带口令密钥的验证流程永不触发。修复：处理器内直接读 `CheckBox.IsChecked` 同步推回 VM.IsActive（CreatePartialStashWindow 模块 14 同款；验证失败回写 false → INPC → 绑定把勾选推回 false → 二次事件收敛，与 WPF 的 Unchecked 再触发语义等价，无循环）。**用例 3 由此转绿**（上轮 agent 排查方向"Parent 未挂接/DataContext 未装配"不成立——CheckBoxOfKey 的查找条件本身要求二者就绪）。**用例 6 挂死根因闭环（上轮 agent "全量 dotnet test 挂起 15 分钟"之谜，dotnet-stack 栈 + 临时探针实证）**：栈显示 UI 线程卡 `UiClick.Click(RemoveWorkspaceButton) → RemoveWorkspace → WindowDialogCompat.ShowDialog` 的 PushFrame 模态泵；探针（E2e23ProbeTests，跑完即删）实证 **`Dispatcher.UIThread.RunJobs()` 默认泵会消费 Background 优先级 job**（`ranInRunJobs=True`）——测试把关闭确认框的 Post(Background) handler 排在 Click 之前、中间又调了一次 `RunJobs()`，handler 在确认框弹出前就被泵执行（msgBox==null 即 return），随后模态框弹出无人关闭 → PushFrame 永久等待 → Run delegate 永不返回 → 挂死（CPU 0%，timeout 强杀 testhost）。PushFrame 嵌套泵本身能执行 Background job（探针 `ranInFrame=True`，模块 5 模式成立的前提）。修复：**选中变化先 `RunJobs()`、Post 之后直接 Click**（模块 5 E2e05 discard 用例的写法——它 Post 与 Click 之间恰好无 RunJobs，故从未踩坑）。**用例 1 断言修正（上轮 "用例 1 ✓" 记录不实——修正后从未在本环境复跑通过）**：空态详情字段保持 XAML 初始值，但三个控件形态有别——SshKeyPathTextBlock/SshKeySha256TextBox 是未设 Text 的 SelectableTextBlock（初始 null）、SshKeyPublicKeyTextBox 是显式 `Text=""` 的 TextBox（初始 ""）；上轮"全 null"结论只核对了 SelectableTextBlock 形态。**六用例覆盖**：① 空态（列表空 + 双 fallback + default system ssh-agent 斜体 + 图标折叠 + Submit → SshKeys 空数组落盘）；② 列表详情（ed25519 真实密钥装配 + Sha256 托管指纹 32 字节 + 选中详情三字段 + 激活 Submit 落盘 + 重开往返从 SshKeys 装配激活态）；③ 勾选验证（生产点击序 → ValidateSshKey 真实 ssh-keygen -y 验证（上轮探测链修复后）→ 配置文本联动/多密钥逗号连接警告/取消回落/Submit 落盘）；④ 生成密钥（空名/空邮箱/重名三态禁用 + 重名状态条警告 + ssh-keygen 命令预览 + 真实生成：GitResult.Success + ResultKey + 私钥/公钥落盘 + 公钥含 email 注释）；⑤ Git 实例（候选装配（ForkPlus 内置 + System PATH 双源真实跑 git --version）+ TextBox↔ListBox 双向联动 + 无效路径清选中禁用 + 有效路径 Submit → GitInstancePath 落盘）；⑥ Workspaces（默认 Home/Work + 删除按钮 count≤2 禁用 → 添加（编辑模式 TextBox Post Background 后入视觉树）→ KeyUp Enter 提交改名 → 删除（MessageBox 确认泵）→ 选中回落相邻项 + ShowInTitle 开关 + OnClosing → Workspaces Update 按名排序 + settings.json 落盘断言）。**上轮 agent 遗留清单处置**：用例 3（已修，生产 bug）；用例 4/5/6（本轮首跑即绿——上轮"未跑过"实为被用例 6 挂死连坐，全量挂起 15 分钟即此）；docs/evidence/ 大量 png 截图刷新（上轮已随 18d2468 提交，本轮 git status 干净无残留）。
- 2026-09-06：模块 24 完成（E2e24AiTests 六用例全绿：Composer WIP 全链路 ✓ / Composer 守卫 ✓ / CodeReview Files 路径 ✓ / CodeReview Branch 路径 ✓ / Dev 模型列表与多轮对话 ✓ / Dev 停止与清除 ✓；截图 24-ai/ 8 张；连跑两轮 6/6 幂等绿，各 19s）。**测试基建：OpenAiStubServer**（沙盒无外网，AI 功能全链路 stub 化）——127.0.0.1 随机端口 HttpListener，实现三个端点：GET /v1/models → 三模型列表（InitializeModelComboBox 后台拉取）、POST /v1/chat/completions body "stream":true → SSE 分片（data: {"choices":[{"delta":{"content":...}}]} × 3 + data: [DONE]，真实走 Connection.RequestStream 逐行读 + ParseSseLine chunk 管线）、body "stream":false → 标准 choices[0].message.content（OpenAiResponse.Decode 路径）。回复经 ChatResponder（Func<请求body, 回复文本>）按 prompt 关键字分发各功能 canned 回复（Composer 返回 ```forkplus-ai-wip-plan JSON 块）；所有请求体/Authorization 头入 Captured 供断言（鉴权 Bearer token、模型名、多轮历史）。配置对接：AiReviewServiceUrl 指向 stub 裸 base URL（NormalizeServiceUrl 剥 /v1 后缀）+ ApiKey "stub-key" 即触发 IsAiReviewConfigured。**六用例覆盖**：① Composer WIP 全链路（CreateAiStaged 工厂 3 staged 文件两组 → staged diff → 流式 SSE → ParseWipCommitPlan JSON 解析 → 左侧 2 分组装配 + 首组自动选中（文件列表/subject/body/reason）+ 切组联动 + Apply All → ComposeWipCommitsGitCommand 逐组 stage+commit 真实落盘（git log 两提交断言 + diff --cached 清空）+ stub 捕获断言 Bearer stub-key 鉴权头/stub-model-a 模型名/split prompt 关键字）；② Composer 守卫（未配置 AI → Loaded → StartAiRequest 守卫弹 "AI is not configured..." 提前 return 零请求；已配置 + 空 staged 数组 → "No staged files to compose..." 守卫——两用例均模态确认泵 + ChatRequestBodies.Count==0 断言守卫在 HTTP 前拦截）；③ CodeReview Files 路径（AiCodeReviewTarget.Files(3 文件) 构造 → 流式审查 → 状态栏清空 + Retry 启用 + Markdown 审查内容装配 + stub 捕获请求体含 src/app.cs）；④ CodeReview Branch 路径（merge-base main feature/two → AiCodeReviewTarget.Branch → 标题 TrFormat("Code review for {0}...{1}", "feature/two", "") 本地化装配 + 审查完成）；⑤ Dev 模型列表与多轮对话（模型下拉后台拉取 + 选中 stub-model-a + SendButton 发送 → 用户消息气泡 + AI 流式响应气泡（MessagePanel ≥3 Border）+ StopButton 隐藏判完成 + 第二条消息验证多轮历史（第二条请求体含首条用户消息））；⑥ Dev 停止与清除（慢响应 200ms 造窗口期 → StopButton 可见 → Click 停止 → 隐藏；ClearConversationButton → MessagePanel 清空 + 欢迎消息重显示）。**测试经验**：a) **模态确认泵按钮必须 Tr() 定位**——守卫 MessageBox 的按钮文案走 PreferencesLocalization，测试环境 UI 语言 zh-Hans 下 "OK"→"确定"，硬编码 "OK" 找不到按钮 → 模态框无人关闭 → PushFrame 永久挂死（首个 agent 挂起根因）；b) **Post(Background) 与 Show() 之间不得插入 RunJobs**——RunJobs 默认泵会消费 Background 优先级 job，handler 在模态框弹出前被消费（msgBox==null 即 return）→ 模态框弹出无人关 → 挂死（模块 23 教训沿用）；c) **Branch 路径标题断言用完整格式键** TrFormat("Code review for {0}...{1}", "feature/two", "")（裸键 Tr("Review") 回退英文原文断言失败——RefreshTitle 按 FormatCurrent 拼装，模块 10 教训）；d) CreateAiStaged 新增 docs/notes.md 前 Directory.CreateDirectory（目录不存在 File.WriteAllText 抛 DirectoryNotFoundException）；e) AI 窗口均依赖 MainWindow.ActiveRepositoryUserControl 走通 JobQueue 入队——须先 OpenRepository 真实开仓再直构窗口（模块 5 模式）。**设置污染防护**：模块 21/23 全属性反射快照沿用（AI 四项设置 ServiceUrl/ApiKey/SelectedModel/RetryCount/TimeoutSeconds/AutoFetchModels 直写 + Save）。**生产 bug：本模块未发现**（Composer 守卫/流式管线/分组装配/多轮历史行为与 WPF 原仓一致）。
