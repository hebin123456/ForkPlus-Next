<UserControl xmlns="https://github.com/avaloniaui" xmlns:controls="clr-namespace:ForkPlus.UI.Controls" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="ForkPlus.UI.UserControls.Preferences.AiReviewPreferencesUserControl">
  <Grid Width="560">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="170" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="Auto" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <TextBlock Grid.Row="0" Grid.Column="0" Text="AI Model Configuration" Theme="{DynamicResource SettingsHeaderTextBlockStyle}" />
    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,8,0,0" VerticalAlignment="Center" HorizontalAlignment="Right" Text="Inference service URL:" />
    <controls:PlaceholderTextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Margin="10,8,0,0" Padding="4,2,4,2" Placeholder="https://api.openai.com" TextChanged="ServiceUrlTextBox_TextChanged" x:Name="ServiceUrlTextBox" />
    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,8,0,0" VerticalAlignment="Center" HorizontalAlignment="Right" Text="API Key:" />
    <controls:PlaceholderTextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="10,8,0,0" Padding="4,2,4,2" Placeholder="API key" TextChanged="ApiKeyTextBox_TextChanged" x:Name="ApiKeyTextBox" />
    <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,8,0,0" VerticalAlignment="Center" HorizontalAlignment="Right" Text="Model:" />
    <ComboBox Grid.Row="3" Grid.Column="1" Margin="10,8,0,0" Height="26" VerticalContentAlignment="Center" IsEditable="True" SelectionChanged="ModelComboBox_SelectionChanged" LostFocus="ModelComboBox_LostFocus" x:Name="ModelComboBox" />
    <Button Grid.Row="3" Grid.Column="2" Margin="6,8,0,0" Width="26" Height="26" Padding="4" ToolTip="Refresh models" Click="RefreshModelsButton_Click" x:Name="RefreshModelsButton">
      <Image Source="{DynamicResource RefreshIcon}" />
    </Button>
    <CheckBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Margin="10,8,0,0" Content="Automatically fetch mounted models from URL and API key" Checked="AutoFetchModelsCheckBox_Changed" Unchecked="AutoFetchModelsCheckBox_Changed" x:Name="AutoFetchModelsCheckBox" />
    <TextBlock Grid.Row="5" Grid.Column="0" Margin="0,8,0,0" VerticalAlignment="Center" HorizontalAlignment="Right" Text="Retry count:" />
    <TextBox Grid.Row="5" Grid.Column="1" Margin="10,8,0,0" Width="48" HorizontalAlignment="Left" TextChanged="RetryCountTextBox_TextChanged" x:Name="RetryCountTextBox" />
    <TextBlock Grid.Row="6" Grid.Column="0" Margin="0,8,0,0" VerticalAlignment="Center" HorizontalAlignment="Right" Text="Max timeout (seconds):" />
    <TextBox Grid.Row="6" Grid.Column="1" Margin="10,8,0,0" Width="48" HorizontalAlignment="Left" TextChanged="TimeoutTextBox_TextChanged" x:Name="TimeoutTextBox" />
    <DockPanel Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2" Margin="10,8,0,0" x:Name="StatusContainer">
      <ProgressBar DockPanel.Dock="Left" Margin="0,0,6,0" Width="14" Height="14" x:Name="BusyIndicator" IsVisible="False">
        <ProgressBar.Style>
          <DynamicResource ResourceKey="CircularProgressBar" />
        </ProgressBar.Style>
      </ProgressBar>
      <TextBlock VerticalAlignment="Center" TextWrapping="Wrap" Foreground="{DynamicResource SecondaryLabelBrush}" x:Name="StatusTextBlock" />
    </DockPanel>
    <!-- Skills section - unified layout matching the menu pattern above -->
    <TextBlock Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="3" Text="Skills" Margin="0,12,0,0" Theme="{DynamicResource SettingsHeaderTextBlockStyle}" />
    
    <!-- Row 9: Skill name input + Add button -->
    <TextBlock Grid.Row="9" Grid.Column="0" Margin="0,8,0,0" VerticalAlignment="Center" HorizontalAlignment="Right" Text="Skill name:" />
    <TextBox Grid.Row="9" Grid.Column="1" Margin="10,8,0,0" Padding="4,2,4,2" VerticalContentAlignment="Center" x:Name="SkillNameTextBox" />
    <Button Grid.Row="9" Grid.Column="2" Margin="6,8,0,0" Padding="8,4,8,4" FontSize="12" x:Name="AddCustomSkillButton">
      <TextBlock Text="Add Skill" TextDecorations="{x:Null}" />
    </Button>
    
    <!-- Row 10: Skill content editor -->
    <TextBlock Grid.Row="10" Grid.Column="0" Margin="0,8,0,0" VerticalAlignment="Top" HorizontalAlignment="Right" Text="Content:" />
    <Border Grid.Row="10" Grid.Column="1" Grid.ColumnSpan="2" Height="120" Margin="10,4,0,0" BorderBrush="{DynamicResource ForkPlusDialogDescriptionForeground}" BorderThickness="1" CornerRadius="2" x:Name="CustomSkillInputBorder" />
    
    <!-- Row 11: Import file button + hint -->
    <TextBlock Grid.Row="11" Grid.Column="0" Margin="0,8,0,0" VerticalAlignment="Center" HorizontalAlignment="Right" Text="Import:" />
    <StackPanel Grid.Row="11" Grid.Column="1" Margin="10,8,0,0" Orientation="Horizontal">
      <Button Padding="8,4,8,4" FontSize="12" x:Name="LoadSkillButton">
        <TextBlock Text="Load Skill File..." TextDecorations="{x:Null}" />
      </Button>
      <TextBlock VerticalAlignment="Center" FontSize="11" Foreground="{DynamicResource SecondaryLabelBrush}" Margin="8,0,0,0" x:Name="CtrlEnterHintTextBlock" Text="Ctrl+Enter to add" />
    </StackPanel>
    
    <!-- Row 12: Loaded skills list (scrollable with max height) -->
    <TextBlock Grid.Row="12" Grid.Column="0" Margin="0,8,0,0" VerticalAlignment="Top" HorizontalAlignment="Right" Text="Loaded skills:" />
    <ListBox Grid.Row="12" Grid.Column="1" Grid.ColumnSpan="2" Margin="10,4,0,0" Height="200" BorderThickness="1" ScrollViewer.VerticalScrollBarVisibility="Auto" x:Name="SkillListBox" />
  </Grid>
</UserControl>
